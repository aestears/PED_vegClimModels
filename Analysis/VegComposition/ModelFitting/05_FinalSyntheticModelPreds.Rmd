---
title: "Generating final model predictions after relativizing"
author: "Alice Stears"
date: "`r lubridate::today()`"
output:
  html_document:
    toc: true
    toc_float: true
    df_print: paged
    code_folding: hide
    dev: "jpeg"
params:
  readParams: TRUE
  runClimate: FALSE
  testFig: FALSE
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning=FALSE,
                      message = FALSE)
```

In this document, we integrate our forest classification model as well as models for each different functional group and ecoregion we considered into CONUS-wide predictions of both absolute and relative cover. We show maps of these model predictions that are generated using contemporary climate, weather, and soils data, and assess how these predictions compare to observed data using maps of residuals, quantile plots, bias, and RMSE. We also show model predictions of both absolute and relative cover made using modeled climate and weather data from the end of the 20th century under RCP 8.5. We used data from two different generalized circulation models (GCMs) from CMIP5: “BNU-ESM”, a cooler and wetter model, and "IPSL-CM5A-MR (France)", a hotter and drier model. For these future predictions of cover, we display maps that show how these future model predictions compare to modeled contemporary cover. We present all of this information (maps of contemporary cover predictions, residuals, quantile plots, RMSE, bias, and maps of future cover based on GCMs) for both absolute and relative cover. 

Setup the environment: 
```{r warning=FALSE, message=FALSE}
library(tidyverse)
library(sf)
library(terra)
library(kableExtra)
library(knitr)
library(USA.state.boundaries)
library(tidyterra)
library(ggpubr)
library(USAboundaries)
library(ggrepel)
# set ggplot theme default 
theme_set(theme_classic())

# Load User defined parameters and functions
print(params)
# set to true if want to run for a limited number of rows (i.e. for code testing)
readParams <- params$readParams
runClimate <- params$runClimate
testFig <- params$testFig
## function to maek predictions
makePredictions <- function(predictionDF, modelObject, scale  = TRUE) {
  ##
  # predictionDF <- climDatPred
  # modelObject <- bestLambdaMod_grassShrub_totalHerb
  # # ##
  # predictionDF = modDat_quantile,
  # modelObject = forb_CONUS, scale = FALSE
  # 
  # pretend to scale the input variables so the model object can predict accurately
  if(scale == TRUE) {
  predictionDF <- predictionDF %>% 
  mutate(across(all_of(prednames), base::scale,scale = FALSE, center = FALSE)) 
  }
  # modelPredictions
  modelPreds <- predict(object = modelObject, newdata = predictionDF, type = "response")
  # add predictions back into the input data.frame
  predictionDF <- predictionDF %>% 
    cbind(modelPreds)
  
  # truncate all predictions to max out at 100 
  #predictionDF[predictionDF$modelPreds>100 & !is.na(predictionDF$modelPreds),"modelPreds"] <- 100
predictionDF[predictionDF$modelPreds < 0 & !is.na(predictionDF$modelPreds),"modelPreds"] <- 0

  # print predicted data
 return(predictionDF)
}

# custom functions for plotting inset figures
            # make function to add inset
           get_inset <- function(df) {
              p <- ggplot(data = df) +
             facet_wrap(~lyr, ncol = 1, scales = "free_y") +
                geom_density(aes(Cover)) +
                theme_bw(base_size=9) +  ## makes everything smaller
                theme(panel.background = element_rect(fill="transparent"),  ## white plot background
                      panel.border = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_blank(),
          axis.text.x = element_text(size=rel(2)), ## tiny axis text
          axis.text.y = element_text(size=rel(2)), ## tiny axis text
          axis.line.x = element_line(size = .5),
          axis.line.y = element_line(size = .5),
          panel.grid.major = element_blank(),
          panel.grid.minor = element_blank(),
          plot.background = element_blank(),
          strip.background = element_blank(),
          strip.text = element_blank()) 
  return(p)
           }
           # custom function to add inset to main plot
           annotation_custom2 <- function (grob, xmin = -Inf, xmax = Inf, ymin = -Inf, ymax = Inf, data) {
  layer(data = data, stat = StatIdentity, position = PositionIdentity,
        geom = ggplot2:::GeomCustomAnn,
        inherit.aes = TRUE, params = list(grob = grob,
                                          xmin = xmin, xmax = xmax,
                                          ymin = ymin, ymax = ymax))
           }
           ## custom inset plot for density plots w/ two lines
                      get_inset_2 <- function(df) {
              p <- ggplot(data = df) +
             facet_wrap(~lyr, ncol = 1, scales = "free_y") +
                geom_density(aes(Cover, col = state)) +
                theme_bw(base_size=9) +  ## makes everything smaller
                theme(panel.background = element_rect(fill="transparent"),  ## white plot background
                      panel.border = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_blank(),
          axis.text.x = element_text(size=rel(2)), ## tiny axis text
          axis.text.y = element_text(size=rel(2)), ## tiny axis text
          axis.line.x = element_line(size = .5),
          axis.line.y = element_line(size = .5),
          panel.grid.major = element_blank(),
          panel.grid.minor = element_blank(),
          plot.background = element_blank(),
          strip.background = element_blank(),
          strip.text = element_blank()) 
  return(p)
           }

                      ## source functions for quantile plots
source("../../../Functions/glmTransformsIterates.R")
source("../../../Functions/transformPreds.R")

## crs of observed data
crs_veg <- "PROJCRS[\"unnamed\",\n    BASEGEOGCRS[\"unknown\",\n        DATUM[\"unknown\",\n            ELLIPSOID[\"Spheroid\",6378137,298.257223563,\n                LENGTHUNIT[\"metre\",1,\n                    ID[\"EPSG\",9001]]]],\n        PRIMEM[\"Greenwich\",0,\n            ANGLEUNIT[\"degree\",0.0174532925199433,\n                ID[\"EPSG\",9122]]]],\n    CONVERSION[\"Lambert Conic Conformal (2SP)\",\n        METHOD[\"Lambert Conic Conformal (2SP)\",\n            ID[\"EPSG\",9802]],\n        PARAMETER[\"Latitude of false origin\",42.5,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8821]],\n        PARAMETER[\"Longitude of false origin\",-100,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8822]],\n        PARAMETER[\"Latitude of 1st standard parallel\",25,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8823]],\n        PARAMETER[\"Latitude of 2nd standard parallel\",60,\n            ANGLEUNIT[\"degree\",0.0174532925199433],\n            ID[\"EPSG\",8824]],\n        PARAMETER[\"Easting at false origin\",0,\n            LENGTHUNIT[\"metre\",1],\n            ID[\"EPSG\",8826]],\n        PARAMETER[\"Northing at false origin\",0,\n            LENGTHUNIT[\"metre\",1],\n            ID[\"EPSG\",8827]]],\n    CS[Cartesian,2],\n        AXIS[\"easting\",east,\n            ORDER[1],\n            LENGTHUNIT[\"metre\",1,\n                ID[\"EPSG\",9001]]],\n        AXIS[\"northing\",north,\n            ORDER[2],\n            LENGTHUNIT[\"metre\",1,\n                ID[\"EPSG\",9001]]]]"
```

```{r}
# Load Data ---------------------------------------------------------------
# data ready for cover modeling (that has been scaled)
modDat_1_s <- readRDS("./models/scaledModelInputData.rds")
## prevalence threshold for the yes/no forest model
best_threshold_bestLambda <- readRDS("./models/ForestModelPrevalence.rds")

# get dayMet raster for map-making and scale it to an 8x8km grid 
test_rastTemp <-  rast("../../../Data_raw/dayMet/rawMonthlyData/orders/70e0da02b9d2d6e8faa8c97d211f3546/Daymet_Monthly_V4R1/data/daymet_v4_prcp_monttl_na_1980.tif") 
#test_rast <- test_rastTemp %>% 
  #terra::aggregate(fact = 12, fun = "mean", na.rm = TRUE)

# get state boundaries for plotting
cropped_states <- us_states(resolution = "low")
cropped_states <- suppressMessages(cropped_states %>%
  dplyr::filter(name!="Hawaii") %>%
  dplyr::filter(name!="Alaska") %>%
  dplyr::filter(name!="Puerto Rico") %>%
  dplyr::filter(name!="American Samoa") %>%
  dplyr::filter(name!="Guam") %>%
  dplyr::filter(name!="Commonwealth of the Northern Mariana Islands") %>%
  dplyr::filter(name!="United States Virgin Islands")  %>%
  sf::st_transform(sf::st_crs(test_rastTemp)))

climDat_Temp <- readRDS("../../../Data_processed/WallToWallClimateData/DayMetData_allCONUS_2023ClimateValues_withSoils.rds")
#climDat_Temp <- readRDS("../../../Data_processed/EcoRegion_climSoilData.rds")

# rename
climDat <- climDat_Temp %>% 
  #filter(year == 2016) %>% 
  dplyr::select(tmin_meanAnnAvg_CLIM:durationFrostFreeDays_meanAnnAvg_CLIM,
                #tmean_meanAnnAvg_3yrAnom:durationFrostFreeDays_meanAnnAvg_3yrAnom,
               soilDepth:totalAvailableWaterHoldingCapacity) %>%
rename("tmin" = tmin_meanAnnAvg_CLIM, 
     "tmax" = tmax_meanAnnAvg_CLIM, #1 
     "tmean" = tmean_meanAnnAvg_CLIM, 
     "prcp" = prcp_meanAnnTotal_CLIM, 
     "t_warm" = T_warmestMonth_meanAnnAvg_CLIM,
     "t_cold" = T_coldestMonth_meanAnnAvg_CLIM, 
     "prcp_wet" = precip_wettestMonth_meanAnnAvg_CLIM,
     "prcp_dry" = precip_driestMonth_meanAnnAvg_CLIM, 
     "prcp_seasonality" = precip_Seasonality_meanAnnAvg_CLIM, #2
     "prcpTempCorr" = PrecipTempCorr_meanAnnAvg_CLIM,  #3
     "abvFreezingMonth" = aboveFreezing_month_meanAnnAvg_CLIM, 
     "isothermality" = isothermality_meanAnnAvg_CLIM, #4
     "annWatDef" = annWaterDeficit_meanAnnAvg_CLIM, 
     "annWetDegDays" = annWetDegDays_meanAnnAvg_CLIM,
     "VPD_mean" = annVPD_mean_meanAnnAvg_CLIM, 
     "VPD_max" = annVPD_max_meanAnnAvg_CLIM, #5
     "VPD_min" = annVPD_min_meanAnnAvg_CLIM, #6
     "VPD_max_95" = annVPD_max_95percentile_CLIM, 
     "annWatDef_95" = annWaterDeficit_95percentile_CLIM, 
     "annWetDegDays_5" = annWetDegDays_5percentile_CLIM, 
     "frostFreeDays_5" = durationFrostFreeDays_5percentile_CLIM, 
     "frostFreeDays" = durationFrostFreeDays_meanAnnAvg_CLIM, 
     "soilDepth" = soilDepth, #7
     "clay" = surfaceClay_perc, 
     "sand" = avgSandPerc_acrossDepth, #8
     "coarse" = avgCoarsePerc_acrossDepth, #9
     "carbon" = avgOrganicCarbonPerc_0_3cm, #10
     "AWHC" = totalAvailableWaterHoldingCapacity,
  )

## data over time (not wall to wall) for examining variation in predictions over time
## contemporary climate data for temporal analysis (NOT wall-to-wall)
climDat_timeTemp <- readRDS("../../../Data_processed/EcoRegion_climSoilData.rds")

climDat_timeTemp <- climDat_timeTemp %>% 
 # mutate("Lon" = st_coordinates(.)[,1],
  #       "Lat" = st_coordinates(.)[,2]) %>% 
  mutate(spatialID = paste0(round(x,4), "_", round(y,4))) %>% 
  drop_na()

spatID_lu <- data.frame("spatialID" = unique(climDat_timeTemp$spatialID), 
                        "uniqueID" = 1:length(unique(climDat_timeTemp$spatialID)))

climDat_timeTemp <- climDat_timeTemp %>% 
  left_join(spatID_lu)

## because of the spatial joining we did, there are some "locations" that have data for less than the true time span we want (2010-2020), or multiple observations per year... remove those so we have the even representation over the time span we're looking for 
set.seed("12011993")
goodTable <- data.frame(table(climDat_timeTemp$uniqueID))
goodIDs_temp <- as.numeric(goodTable[goodTable$Freq == 11,"Var1"])

#goodIDs <- unique(names(table(climDat_timeTemp$uniqueID))[(table(climDat_timeTemp$uniqueID)==11)])
# select 1000 random IDs
goodIDs <- goodIDs_temp[sample(x = 1:length(goodIDs_temp), size = 1000, replace = FALSE)]

## get climate data for 500 different points randomly chosen across CONUS for all years in the contemporary climate/weather/soils dataset 
climDat_time <- climDat_timeTemp %>% 
  filter(uniqueID %in% goodIDs) %>% 
  dplyr::select(tmin_meanAnnAvg_CLIM:durationFrostFreeDays_meanAnnAvg_3yrAnom, NA_L1CODE, 
         NA_L1NAME, NA_L1KEY, year, newRegion, x, y, soilDepth:totalAvailableWaterHoldingCapacity, uniqueID, spatialID) %>% 
rename("tmin" = tmin_meanAnnAvg_CLIM, 
     "tmax" = tmax_meanAnnAvg_CLIM, #1 
     "tmean" = tmean_meanAnnAvg_CLIM, 
     "prcp" = prcp_meanAnnTotal_CLIM, 
     "t_warm" = T_warmestMonth_meanAnnAvg_CLIM,
     "t_cold" = T_coldestMonth_meanAnnAvg_CLIM, 
     "prcp_wet" = precip_wettestMonth_meanAnnAvg_CLIM,
     "prcp_dry" = precip_driestMonth_meanAnnAvg_CLIM, 
     "prcp_seasonality" = precip_Seasonality_meanAnnAvg_CLIM, #2
     "prcpTempCorr" = PrecipTempCorr_meanAnnAvg_CLIM,  #3
     "abvFreezingMonth" = aboveFreezing_month_meanAnnAvg_CLIM, 
     "isothermality" = isothermality_meanAnnAvg_CLIM, #4
     "annWatDef" = annWaterDeficit_meanAnnAvg_CLIM, 
     "annWetDegDays" = annWetDegDays_meanAnnAvg_CLIM,
     "VPD_mean" = annVPD_mean_meanAnnAvg_CLIM, 
     "VPD_max" = annVPD_max_meanAnnAvg_CLIM, #5
     "VPD_min" = annVPD_min_meanAnnAvg_CLIM, #6
     "VPD_max_95" = annVPD_max_95percentile_CLIM, 
     "annWatDef_95" = annWaterDeficit_95percentile_CLIM, 
     "annWetDegDays_5" = annWetDegDays_5percentile_CLIM, 
     "frostFreeDays_5" = durationFrostFreeDays_5percentile_CLIM, 
     "frostFreeDays" = durationFrostFreeDays_meanAnnAvg_CLIM, 
     "soilDepth" = soilDepth, #7
     "clay" = surfaceClay_perc, 
     "sand" = avgSandPerc_acrossDepth, #8
     "coarse" = avgCoarsePerc_acrossDepth, #9
     "carbon" = avgOrganicCarbonPerc_0_3cm, #10
     "AWHC" = totalAvailableWaterHoldingCapacity,
     ## anomaly variables
     tmean_anom = tmean_meanAnnAvg_3yrAnom, #15
     tmin_anom = tmin_meanAnnAvg_3yrAnom, #16
     tmax_anom = tmax_meanAnnAvg_3yrAnom, #17
    prcp_anom = prcp_meanAnnTotal_3yrAnom, #18
      t_warm_anom = T_warmestMonth_meanAnnAvg_3yrAnom,  #19
     t_cold_anom = T_coldestMonth_meanAnnAvg_3yrAnom, #20
      prcp_wet_anom = precip_wettestMonth_meanAnnAvg_3yrAnom, #21
      precp_dry_anom = precip_driestMonth_meanAnnAvg_3yrAnom,  #22
    prcp_seasonality_anom = precip_Seasonality_meanAnnAvg_3yrAnom, #23 
     prcpTempCorr_anom = PrecipTempCorr_meanAnnAvg_3yrAnom, #24
      aboveFreezingMonth_anom = aboveFreezing_month_meanAnnAvg_3yrAnom, #25  
    isothermality_anom = isothermality_meanAnnAvg_3yrAnom, #26
       annWatDef_anom = annWaterDeficit_meanAnnAvg_3yrAnom, #27
     annWetDegDays_anom = annWetDegDays_meanAnnAvg_3yrAnom,  #28
      VPD_mean_anom = annVPD_mean_meanAnnAvg_3yrAnom, #29
      VPD_min_anom = annVPD_min_meanAnnAvg_3yrAnom,  #30
      VPD_max_anom = annVPD_max_meanAnnAvg_3yrAnom,  #31
     VPD_max_95_anom = annVPD_max_95percentile_3yrAnom, #32
      annWatDef_95_anom = annWaterDeficit_95percentile_3yrAnom, #33 
      annWetDegDays_5_anom = annWetDegDays_5percentile_3yrAnom ,  #34
    frostFreeDays_5_anom = durationFrostFreeDays_5percentile_3yrAnom, #35 
      frostFreeDays_anom = durationFrostFreeDays_meanAnnAvg_3yrAnom #36
  ) %>% 
  dplyr::select(-c(tmin_meanAnnAvg_3yr:Start_3yr)) 

if (runClimate == TRUE) {
## contemporary climate data (wall-to-wall, no anomalies)

#rm(climDat_Temp) 
#gc()

## now, scale the contemporary climate data for use in cover models 
# get the scaling factors 
scaleParams <- modDat_1_s %>% 
  #filter(Year == 2016) %>% 
  dplyr::select(tmin_s:AWHC_s) %>% 
  reframe(across(all_of(names(.)), attributes)) 

# apply the scaling factors to the contemporary climate data 
climDat <- climDat %>% 
  mutate("Lon" = st_coordinates(.)[,1],
         "Lat" = st_coordinates(.)[,2])

climDat_noSF <- climDat %>% 
  st_drop_geometry()

namesToScale <- climDat_noSF %>% 
  dplyr::select(tmin:frostFreeDays, soilDepth:carbon, AWHC) %>% 
  names()

climDat_scaled <- map(namesToScale, .f = function(x) {
  x_new <- (climDat_noSF[,x] - scaleParams[,paste0(x, "_s")]$`scaled:center`)/scaleParams[,paste0(x, "_s")]$`scaled:scale`
  return(data.frame(x_new))
}) %>% 
  purrr::list_cbind()
names(climDat_scaled) <- paste0(namesToScale, "_s")

climDatPred <- climDat %>% 
  dplyr::select(Lon, Lat, geometry) %>% 
  cbind(climDat_scaled)
names(climDatPred)[3:31] <- str_remove(names(climDatPred)[3:31], pattern = "_s$")

# the wall-to-wall contemporary climate data doesn't have data for anomalies, so we add 0s for all anomalies
climDatPred <- climDatPred %>% 
  mutate(
     tmean_anom = 0, 
     tmin_anom = 0, 
     tmax_anom = 0, 
    prcp_anom = 0, 
      t_warm_anom = 0,  
     t_cold_anom = 0, 
      prcp_wet_anom = 0,
      precp_dry_anom = 0,  
    prcp_seasonality_anom = 0, 
     prcpTempCorr_anom = 0, 
      aboveFreezingMonth_anom = 0, 
    isothermality_anom = 0, 
       annWatDef_anom = 0, 
     annWetDegDays_anom = 0,  
      VPD_mean_anom = 0, 
      VPD_min_anom = 0,  
      VPD_max_anom = 0,  
     VPD_max_95_anom = 0, 
      annWatDef_95_anom = 0, 
      annWetDegDays_5_anom = 0 ,  
    frostFreeDays_5_anom = 0, 
      frostFreeDays_anom = 0
  )
    
## save the wall-to-wall climate data after scaling
saveRDS(climDatPred, "../../../Data_processed/WallToWallClimateData/DayMetData_allCONUS_2023_SCALEDcliamteAndSoilsPredictors.rds")


# apply the scaling factors to the contemporary climate data used for predictions over time 
namesToScale <- climDat_time %>% 
  select(tmin:frostFreeDays,
         tmean_anom:frostFreeDays_anom,
         soilDepth:AWHC) %>% 
  names()

climDat_scaled <- map(namesToScale, .f = function(x) {
  x_new <- (climDat_time[,x] - scaleParams[,paste0(x, "_s")]$`scaled:center`)/scaleParams[,paste0(x, "_s")]$`scaled:scale`
  return(data.frame(x_new))
}) %>% 
  purrr::list_cbind()
names(climDat_scaled) <- paste0(namesToScale, "_s")

climDat_time_Pred <- climDat_time %>% 
  dplyr::select(NA_L1CODE:y,spatialID) %>% 
  cbind(climDat_scaled)
names(climDat_time_Pred)[9:58] <- str_remove(names(climDat_time_Pred)[9:58], pattern = "_s$")

rm(climDat_scaled, climDat_Temp, climDat_timeTemp) 
gc()

## save scaled climate data for predictions over time
saveRDS(climDat_time_Pred, "../../../Data_processed/ClimateWeatherSoilsDataForPredictionsAcrossTime.rds")


## read in the unscaled data for future predicted climate - model 1
future1_points <- readRDS("../../../Data_processed/WallToWallClimateData/BNU_ESM_1i1p1_rcp85_2086_2099/ForecastedClimateData_BNU-ESM_PointsWithSoils.rds")

## update names to be consistent with what we use in this script
future1_points <- future1_points %>% 
  rename("tmin" = tmin_meanAnnAvg_CLIM, 
     "tmax" = tmax_meanAnnAvg_CLIM, #1 
     "tmean" = tmean_meanAnnAvg_CLIM, 
     "prcp" = prcp_meanAnnTotal_CLIM, 
     "t_warm" = T_warmestMonth_meanAnnAvg_CLIM,
     "t_cold" = T_coldestMonth_meanAnnAvg_CLIM, 
     "prcp_wet" = precip_wettestMonth_meanAnnAvg_CLIM,
     "prcp_dry" = precip_driestMonth_meanAnnAvg_CLIM, 
     "prcp_seasonality" = precip_Seasonality_meanAnnAvg_CLIM, #2
     "prcpTempCorr" = PrecipTempCorr_meanAnnAvg_CLIM,  #3
     "abvFreezingMonth" = aboveFreezing_month_meanAnnAvg_CLIM, 
     "isothermality" = isothermality_meanAnnAvg_CLIM, #4
     "annWatDef" = annWaterDeficit_meanAnnAvg_CLIM, 
     "annWetDegDays" = annWetDegDays_meanAnnAvg_CLIM,
     "VPD_mean" = annVPD_mean_meanAnnAvg_CLIM, 
     "VPD_max" = annVPD_max_meanAnnAvg_CLIM, #5
     "VPD_min" = annVPD_min_meanAnnAvg_CLIM, #6
     "VPD_max_95" = annVPD_max_95percentile_CLIM, 
     "annWatDef_95" = annWaterDeficit_95percentile_CLIM, 
     "annWetDegDays_5" = annWetDegDays_5percentile_CLIM, 
     "frostFreeDays_5" = durationFrostFreeDays_5percentile_CLIM, 
     "frostFreeDays" = durationFrostFreeDays_meanAnnAvg_CLIM, 
     "soilDepth" = soilDepth, #7
     "clay" = surfaceClay_perc, 
     "sand" = avgSandPerc_acrossDepth, #8
     "coarse" = avgCoarsePerc_acrossDepth, #9
     "carbon" = avgOrganicCarbonPerc_0_3cm, #10
     "AWHC" = totalAvailableWaterHoldingCapacity ) 
## center and scale these variables 
# apply the scaling factors to the contemporary climate data used for predictions over time 
future1_points_noSF <- future1_points %>% 
  st_drop_geometry()

namesToScale <- future1_points_noSF %>% 
  select(tmin:AWHC) %>% 
  names()

future1_points_scaled <- map(namesToScale, .f = function(x) {
  x_new <- (future1_points_noSF[,x] - scaleParams[,paste0(x, "_s")]$`scaled:center`)/scaleParams[,paste0(x, "_s")]$`scaled:scale`
  return(data.frame(x_new))
}) %>% 
  purrr::list_cbind()
names(future1_points_scaled) <- paste0(namesToScale, "_s")

forecastClimSoilsDatPred_1 <- future1_points %>% 
  dplyr::select(geometry) %>% 
  cbind(future1_points_scaled)
names(forecastClimSoilsDatPred_1)[1:28] <- str_remove(names(forecastClimSoilsDatPred_1)[1:28], pattern = "_s$")

rm(future1_points_scaled, future1_points_noSF, future1_points) 
gc()

## read in the unscaled data for future predicted climate - model 2

future2_points <- readRDS( "../../../Data_processed/WallToWallClimateData/IPSL-CM5A-MR_r1i1p1_rcp85_2086_2099/ForecastedClimateData_IPSL-CM5A-MR_PointsWithSoils.rds")

## update names to be consistent with what we use in this script
future2_points <- future2_points %>% 
  rename("tmin" = tmin_meanAnnAvg_CLIM, 
     "tmax" = tmax_meanAnnAvg_CLIM, #1 
     "tmean" = tmean_meanAnnAvg_CLIM, 
     "prcp" = prcp_meanAnnTotal_CLIM, 
     "t_warm" = T_warmestMonth_meanAnnAvg_CLIM,
     "t_cold" = T_coldestMonth_meanAnnAvg_CLIM, 
     "prcp_wet" = precip_wettestMonth_meanAnnAvg_CLIM,
     "prcp_dry" = precip_driestMonth_meanAnnAvg_CLIM, 
     "prcp_seasonality" = precip_Seasonality_meanAnnAvg_CLIM, #2
     "prcpTempCorr" = PrecipTempCorr_meanAnnAvg_CLIM,  #3
     "abvFreezingMonth" = aboveFreezing_month_meanAnnAvg_CLIM, 
     "isothermality" = isothermality_meanAnnAvg_CLIM, #4
     "annWatDef" = annWaterDeficit_meanAnnAvg_CLIM, 
     "annWetDegDays" = annWetDegDays_meanAnnAvg_CLIM,
     "VPD_mean" = annVPD_mean_meanAnnAvg_CLIM, 
     "VPD_max" = annVPD_max_meanAnnAvg_CLIM, #5
     "VPD_min" = annVPD_min_meanAnnAvg_CLIM, #6
     "VPD_max_95" = annVPD_max_95percentile_CLIM, 
     "annWatDef_95" = annWaterDeficit_95percentile_CLIM, 
     "annWetDegDays_5" = annWetDegDays_5percentile_CLIM, 
     "frostFreeDays_5" = durationFrostFreeDays_5percentile_CLIM, 
     "frostFreeDays" = durationFrostFreeDays_meanAnnAvg_CLIM, 
     "soilDepth" = soilDepth, #7
     "clay" = surfaceClay_perc, 
     "sand" = avgSandPerc_acrossDepth, #8
     "coarse" = avgCoarsePerc_acrossDepth, #9
     "carbon" = avgOrganicCarbonPerc_0_3cm, #10
     "AWHC" = totalAvailableWaterHoldingCapacity ) 
## center and scale these variables 
# apply the scaling factors to the contemporary climate data used for predictions over time 
future2_points_noSF <- future2_points %>% 
  st_drop_geometry()

namesToScale <- future2_points_noSF %>% 
  select(tmin:AWHC) %>% 
  names()

future2_points_scaled <- map(namesToScale, .f = function(x) {
  x_new <- (future2_points_noSF[,x] - scaleParams[,paste0(x, "_s")]$`scaled:center`)/scaleParams[,paste0(x, "_s")]$`scaled:scale`
  return(data.frame(x_new))
}) %>% 
  purrr::list_cbind()
names(future2_points_scaled) <- paste0(namesToScale, "_s")

forecastClimSoilsDatPred_2 <- future2_points %>% 
  dplyr::select(geometry) %>% 
  cbind(future2_points_scaled)
names(forecastClimSoilsDatPred_2)[1:28] <- str_remove(names(forecastClimSoilsDatPred_2)[1:28], pattern = "_s$")

rm(future2_points_scaled, future2_points_noSF, future2_points) 
gc()

## save scaled future climate data
saveRDS(forecastClimSoilsDatPred_1, "../../../Data_processed/WallToWallClimateData/BNU_ESM_1i1p1_rcp85_2086_2099/ForecastedClimateData_BNU-ESM_PointsWithSoils_SCALED.rds")

saveRDS(forecastClimSoilsDatPred_2, "../../../Data_processed/WallToWallClimateData/IPSL-CM5A-MR_r1i1p1_rcp85_2086_2099/ForecastedClimateData_IPSL-CM5A-MR_PointsWithSoils_SCALED.rds")

#
} else {
  # read in pre-processed climate data
  ## read in the wall-to-wall climate data after scaling
climDatPred <- readRDS( "../../../Data_processed/WallToWallClimateData/DayMetData_allCONUS_2023_SCALEDcliamteAndSoilsPredictors.rds")
## read scaled cliamte data for predictions over time
climDat_time_Pred <- readRDS("../../../Data_processed/ClimateWeatherSoilsDataForPredictionsAcrossTime.rds")

## read in scaled future climate data
forecastClimSoilsDatPred_1 <- readRDS("../../../Data_processed/WallToWallClimateData/BNU_ESM_1i1p1_rcp85_2086_2099/ForecastedClimateData_BNU-ESM_PointsWithSoils_SCALED.rds")

forecastClimSoilsDatPred_2 <- readRDS("../../../Data_processed/WallToWallClimateData/IPSL-CM5A-MR_r1i1p1_rcp85_2086_2099/ForecastedClimateData_IPSL-CM5A-MR_PointsWithSoils_SCALED.rds")

}


prednames_s <-  modDat_1_s %>%
  dplyr::select(tmin_s:AWHC_s) %>%
  names()
prednames <- str_replace(prednames_s, pattern = "_s$", replacement = "")

#   # read in unscaled data for future climate
#  forecastClimSoilsDat_1 <- readRDS("../../../Data_processed/CoverData/IntermediateAnalysisFiles/Final_ForecastedClimateDataAndSoilsDataForPredictions_BNU-ESM_rcp8_5_UNSCALED.rds")
# forecastClimSoilsDat_2 <- readRDS("../../../Data_processed/CoverData/IntermediateAnalysisFiles/Final_ForecastedClimateDataAndSoilsDataForPredictions_IPSL-CM5A-MR_rcp8_5_UNSCALED.rds")
# add empty anomaly info to future forecast data
forecastClimSoilsDatPred_1 <- forecastClimSoilsDatPred_1 %>% 
  mutate(
     tmean_anom = 0, 
     tmin_anom = 0, 
     tmax_anom = 0, 
    prcp_anom = 0, 
      t_warm_anom = 0,  
     t_cold_anom = 0, 
      prcp_wet_anom = 0,
      precp_dry_anom = 0,  
    prcp_seasonality_anom = 0, 
     prcpTempCorr_anom = 0, 
      aboveFreezingMonth_anom = 0, 
    isothermality_anom = 0, 
       annWatDef_anom = 0, 
     annWetDegDays_anom = 0,  
      VPD_mean_anom = 0, 
      VPD_min_anom = 0,  
      VPD_max_anom = 0,  
     VPD_max_95_anom = 0, 
      annWatDef_95_anom = 0, 
      annWetDegDays_5_anom = 0 ,  
    frostFreeDays_5_anom = 0, 
      frostFreeDays_anom = 0
  )

forecastClimSoilsDatPred_2 <- forecastClimSoilsDatPred_2 %>% 
  mutate(
     tmean_anom = 0, 
     tmin_anom = 0, 
     tmax_anom = 0, 
    prcp_anom = 0, 
      t_warm_anom = 0,  
     t_cold_anom = 0, 
      prcp_wet_anom = 0,
      precp_dry_anom = 0,  
    prcp_seasonality_anom = 0, 
     prcpTempCorr_anom = 0, 
      aboveFreezingMonth_anom = 0, 
    isothermality_anom = 0, 
       annWatDef_anom = 0, 
     annWetDegDays_anom = 0,  
      VPD_mean_anom = 0, 
      VPD_min_anom = 0,  
      VPD_max_anom = 0,  
     VPD_max_95_anom = 0, 
      annWatDef_95_anom = 0, 
      annWetDegDays_5_anom = 0 ,  
    frostFreeDays_5_anom = 0, 
      frostFreeDays_anom = 0
  )
# ggplot() + 
#                  geom_density(data = climDatPred, aes(x = tmean*prcpTempCorr)) +
#   geom_density(data = forecastClimSoilsDatPred_1, aes(x = tmean*prcpTempCorr), col = "red") +
#   geom_density(data = forecastClimSoilsDatPred_2, aes(x = tmean*prcpTempCorr), col = "blue")
# #"tmean" "prcp"  "prcpTempCorr"   "isothermality"  "clay"  "coarse" "AWHC" "I(tmean^2)"   "I(prcp^2)"                   "I(prcpTempCorr^2)"           "I(isothermality^2)"   "I(clay^2)"  "I(AWHC^2)"  "isothermality:annWetDegDays" "prcp:annWetDegDays" "prcpTempCorr:annWetDegDays"  "prcp:isothermality"   "prcpTempCorr:isothermality"  "tmean:isothermality"        "prcp:prcpTempCorr"   
# #"tmean:prcp"  "tmean:prcpTempCorr"   "clay:AWHC"                  
# #"coarse:AWHC"    "clay:coarse"  

if(testFig == TRUE) {
  test_rastTemp <- terra::crop(test_rastTemp, ext(-500000, 500000, -500000, 500000))
  
  climDatPred <- climDatPred %>% 
    dplyr::slice_sample(n = 500000)
  
  forecastClimSoilsDatPred_1 <- forecastClimSoilsDatPred_1 %>% 
    dplyr::slice_sample(n = 50000)
  
  forecastClimSoilsDatPred_2 <- forecastClimSoilsDatPred_2 %>% 
    dplyr::slice_sample(n = 50000)
}

```

# First, we show predictions from the model of forest classification

These maps show predictions of forest classification using contemporary and modeled future climate data, where the predicted value is the forest classification. We arrive at these classification values by translating the continuous probability of forest that was predicted by a binomial generalized linear model to binary values using a cutoff of 0.382 (values greater than 0.382 are forest, while values less than or equal to 0.382 are not forest). This threshold is derived from the average prevalence of forest in the data used to fit the binomial model. 
Note that while the climate data used in model predictions is either contemporary or from GCMs, the soils data used is static across time. 
```{r}
# read in model
forestMod <- fit_glm_bestLambda <- readRDS("./models/ForestMod_bestLambdaGLM_10.rds")
# get threshold for making predictions binary
best_threshold_bestLambda <- readRDS( "./models/ForestModelPrevalence.rds")

## print out model statement
coefficientTable <- data.frame(coefficients(forestMod))
responseVar <- "P(forest)"
coefficientTable$coefficientName <- rownames(coefficientTable)
coefficientTable$coefficients.forestMod. <- round(coefficientTable$coefficients.forestMod., 6)
  
  # print out coefficients w/ coefficient names
tempNames <- paste0(
  apply(coefficientTable, MARGIN = 1, FUN = function(x) {
    if (x["coefficientName"] == "(Intercept)") {
      paste0(x["coefficients.forestMod."])
    } else {  
      paste0(x["coefficients.forestMod."], "*", x["coefficientName"])
    }
    }
  ),
  collapse = " + "
)

# print the unscaled model statement
  unscaledModelName <- paste0(responseVar, "~ 1/ (1 + exp(-(", tempNames,")))")
#print(unscaledModelName)
```
 
```{r fig.width =10, fig.height = 20, message = FALSE}
# Here are maps of the ecoregion model predictions with both contemporary and modeled future climate data 

# get climate data from dayMet (d.f. is "climDatPred_unscaled")
rm(climDat)
gc()
#gc()
preds_byHand <- makePredictions(modelObject = forestMod, predictionDF = climDatPred, scale = TRUE) %>% 
  rename( "pred" = "modelPreds" )
           
  # mutate(pred =  as.numeric(1/ (1 + exp(
  #   -(3.38565522 + -0.79822221*((tmean - 10.6678425) / 5.14823079) +  4.74536062*((prcp - 767.94900623) / 506.45362111) + -0.49798680*((prcpTempCorr - -0.02007952) / 0.40677333) +  0.22745494*((isothermality - 36.51862827) / 5.41402375) + -0.39051271*((clay - 17.45502795) / 8.23327615) +  0.53857137*((coarse - 11.18397843) / 11.24234211) + -0.40059849*((AWHC - 14.91029339) / 5.4856826) + -0.83406990*I(((prcp - 767.94900623) / 506.45362111)^2) + -0.94850342*I(((prcpTempCorr - -0.02007952) / 0.40677333)^2) +  0.19450621*I(((isothermality - 36.51862827) / 5.41402375)^2) + -0.04772718*I(((clay - 17.45502795) / 8.23327615)^2) + -0.26519959*I(((coarse - 11.18397843) / 11.24234211)^2) +  0.07438930*I(((AWHC - 14.91029339) / 5.4856826)^2) + -0.29049829*((prcp - 767.94900623) / 506.45362111)*((isothermality - 36.51862827) / 5.41402375) + -0.16800569*((tmean - 10.6678425) / 5.14823079)*((isothermality - 36.51862827) / 5.41402375) +  0.07625545*((tmean - 10.6678425) / 5.14823079)*((prcp - 767.94900623) / 506.45362111) + -0.55536190*((tmean - 10.6678425) / 5.14823079)*((prcpTempCorr - -0.02007952) / 0.40677333) + -0.37720980*((clay - 17.45502795) / 8.23327615)*((AWHC - 14.91029339) / 5.4856826)
  #     )
  #                                       )
  #   )))

preds_byHand <- preds_byHand %>% 
 # mutate(pred_interp = replace(pred_interp, list = pred < 0.136147, 0)) %>% 
   mutate(pred_binary = pred,
    pred_binary = replace(pred_binary, pred_binary > best_threshold_bestLambda, 1), # now, make the interpolated continuous model predictions binary
         pred_binary = replace(pred_binary, pred_binary <= best_threshold_bestLambda, 0)) 
# now, translate the continuous predictions to correspond to the 'buffer zone' that we want to use (currently best_threshold_bestLambda +/- 0.1) to come up with weighted predictions around the transition from forest to non-forest
# make an interpolation function
interpFun_forest <- stats::approxfun(x = c(0, 0.3216, best_threshold_bestLambda, 0.6213, 1), # values we're interpolating *from*
          y =c(0, 0, 0.5, 1, 1), # what we're interpolation to (desired values at the end)
          method = "linear")
preds_byHand <- preds_byHand %>% 
  mutate(pred_contNew = interpFun_forest(pred))

# ## ecdf of the un-interpolated continuous predictions
# pred_cdf <- stats::ecdf(preds_byHand$pred)
# ## find the quantile of observations that corresponds to the desired proportion of unforested locations
# #zeroCutoff_1seLambda <- quantile(pred_glm1_1se_w2w$TotalTreeCover_binom_pred %>% na.omit(), probs = 0.3133397) 
# # plot the ecdf
# plot(pred_cdf, xlab = c("P(Total Tree Cover)"), ylab = "Percentile",
#      main = "Empirical Cumulative Distribution Function for \nUn-transformed model predictions of \nP(Forest)")
# abline(v = best_threshold_bestLambda, lty = 2)
# text(x = best_threshold_bestLambda+.05, y = 0.65, labels = round(best_threshold_bestLambda, 4))
# abline(v = best_threshold_bestLambda - 0.1, lty = 3)
# text(x = best_threshold_bestLambda - 0.1 +.05, y = 0.55, labels = round(best_threshold_bestLambda - 0.1, 4))
# abline(v = best_threshold_bestLambda + 0.1, lty = 3)
# text(x = best_threshold_bestLambda + 0.1 +.05, y = 0.75, labels = round(best_threshold_bestLambda + 0.1, 4))
# 
# ## ecdf of the interpolated continuous predictions
# pred_conNew_cdf <- stats::ecdf(preds_byHand$pred_contNew)
# ## find the quantile of observations that corresponds to the desired proportion of unforested locations
# #zeroCutoff_1seLambda <- quantile(pred_glm1_1se_w2w$TotalTreeCover_binom_pred %>% na.omit(), probs = 0.3133397) 
# # plot the ecdf
# plot(pred_conNew_cdf, xlab = c("P(Total Tree Cover)"), ylab = "Percentile",
#      main = "Empirical Cumulative Distribution Function for \nTransformed model predictions of \nP(Forest)")

# predict with modeled future climate data v1
preds_future1_byHand <- makePredictions(modelObject = forestMod, predictionDF = forecastClimSoilsDatPred_1, scale = TRUE) %>%
  rename( "pred" = "modelPreds" )
    # mutate(pred =  as.numeric(1/ (1 + exp(-(3.38565522 + -0.79822221*((tmean - 10.6678425) / 5.14823079) +  4.74536062*((prcp - 767.94900623) / 506.45362111) + -0.49798680*((prcpTempCorr - -0.02007952) / 0.40677333) +  0.22745494*((isothermality - 36.51862827) / 5.41402375) + -0.39051271*((clay - 17.45502795) / 8.23327615) +  0.53857137*((coarse - 11.18397843) / 11.24234211) + -0.40059849*((AWHC - 14.91029339) / 5.4856826) + -0.83406990*I(((prcp - 767.94900623) / 506.45362111)^2) + -0.94850342*I(((prcpTempCorr - -0.02007952) / 0.40677333)^2) +  0.19450621*I(((isothermality - 36.51862827) / 5.41402375)^2) + -0.04772718*I(((clay - 17.45502795) / 8.23327615)^2) + -0.26519959*I(((coarse - 11.18397843) / 11.24234211)^2) +  0.07438930*I(((AWHC - 14.91029339) / 5.4856826)^2) + -0.29049829*((prcp - 767.94900623) / 506.45362111)*((isothermality - 36.51862827) / 5.41402375) + -0.16800569*((tmean - 10.6678425) / 5.14823079)*((isothermality - 36.51862827) / 5.41402375) +  0.07625545*((tmean - 10.6678425) / 5.14823079)*((prcp - 767.94900623) / 506.45362111) + -0.55536190*((tmean - 10.6678425) / 5.14823079)*((prcpTempCorr - -0.02007952) / 0.40677333) + -0.37720980*((clay - 17.45502795) / 8.23327615)*((AWHC - 14.91029339) / 5.4856826))))))
preds_future1_byHand <- preds_future1_byHand %>% 
  #mutate(pred_interp = approxfun_forest(pred)) %>% 
 # mutate(pred_interp = replace(pred_interp, list = pred < 0.136147, 0)) %>% 
   mutate(pred_binary = pred,
    pred_binary = replace(pred_binary, pred_binary >best_threshold_bestLambda, 1), # now, make the interpolated continuous model predictions binary
         pred_binary = replace(pred_binary, pred_binary <= best_threshold_bestLambda, 0)) %>% 
  mutate(pred_contNew = interpFun_forest(pred))

# predict with modeled future climate data v2
preds_future2_byHand <-  makePredictions(modelObject = forestMod, predictionDF = forecastClimSoilsDatPred_2, scale = TRUE) %>%
  rename( "pred" = "modelPreds" )
  # mutate(pred =  as.numeric(1/ (1 + exp(-(3.38565522 + -0.79822221*((tmean - 10.6678425) / 5.14823079) +  4.74536062*((prcp - 767.94900623) / 506.45362111) + -0.49798680*((prcpTempCorr - -0.02007952) / 0.40677333) +  0.22745494*((isothermality - 36.51862827) / 5.41402375) + -0.39051271*((clay - 17.45502795) / 8.23327615) +  0.53857137*((coarse - 11.18397843) / 11.24234211) + -0.40059849*((AWHC - 14.91029339) / 5.4856826) + -0.83406990*I(((prcp - 767.94900623) / 506.45362111)^2) + -0.94850342*I(((prcpTempCorr - -0.02007952) / 0.40677333)^2) +  0.19450621*I(((isothermality - 36.51862827) / 5.41402375)^2) + -0.04772718*I(((clay - 17.45502795) / 8.23327615)^2) + -0.26519959*I(((coarse - 11.18397843) / 11.24234211)^2) +  0.07438930*I(((AWHC - 14.91029339) / 5.4856826)^2) + -0.29049829*((prcp - 767.94900623) / 506.45362111)*((isothermality - 36.51862827) / 5.41402375) + -0.16800569*((tmean - 10.6678425) / 5.14823079)*((isothermality - 36.51862827) / 5.41402375) +  0.07625545*((tmean - 10.6678425) / 5.14823079)*((prcp - 767.94900623) / 506.45362111) + -0.55536190*((tmean - 10.6678425) / 5.14823079)*((prcpTempCorr - -0.02007952) / 0.40677333) + -0.37720980*((clay - 17.45502795) / 8.23327615)*((AWHC - 14.91029339) / 5.4856826))))))
preds_future2_byHand <- preds_future2_byHand %>% 
  #mutate(pred_interp = approxfun_forest(pred)) %>% 
  #mutate(pred_interp = replace(pred_interp, list = pred < 0.136147, 0)) %>% 
   mutate(pred_binary = pred,
    pred_binary = replace(pred_binary, pred_binary >best_threshold_bestLambda, 1), # now, make the interpolated continuous model predictions binary
         pred_binary = replace(pred_binary, pred_binary <= best_threshold_bestLambda, 0)) %>% 
  mutate(pred_contNew = interpFun_forest(pred))


## Plot predictions with contemporary data
# make into a raster
plotObs <- preds_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_binary", 
                   fun = mean, na.rm = TRUE) 

# get the extent of this particular raster, and crop≠ it accordingly
tempExt <- crds(plotObs, na.rm = TRUE)

plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )


## save the raster of forest predictions on the dayMet scale w/ contemporary data to use elsewhere
terra::writeRaster(plotObs_2, filename = "../../../Data_processed/CoverData/finalCoverData/predictionsOfForestProbOnDaymetScale.tif", overwrite = TRUE)


map_ecoRegion_current <- 
  ggplot() +
geom_raster(data = plotObs_2, aes(x = x, 
                   y = y, 
                   fill = as.character(mean)), maxcell = 5e+20) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
  scale_fill_manual(name = "", 
                     values = c("tan", "darkgreen"), 
                     labels = c("Non-Forest", "Forest"),
                    na.translate = FALSE) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
  labs(title = c("Forest classification model predictions \nusing contemporary dayMet data \n(from 2023)"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "")

# for future v1
plotObs_future1 <- preds_future1_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect() %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_binary", 
                   fun = mean, na.rm = TRUE)  
    
  
# get the extent of this particular raster, and crop it accordingly
plotObs_future1_2 <- plotObs_future1 %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )

map_ecoRegion_future1 <- 
  ggplot() +
geom_raster(data = plotObs_future1_2, aes(x = x, 
                   y = y, 
                   fill = as.character(mean)), maxcell = 5e+20) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
  scale_fill_manual(name = "", 
                     values = c("tan", "darkgreen"), 
                     labels = c("Non-Forest", "Forest"),
                    na.translate = FALSE) +
  
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
 labs(title = c("Forest classification predictions \nusing future climate data from the \nBNU-ESM model"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     )
             

# for future v2
plotObs_future2 <- preds_future2_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect() %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_binary", 
                   fun = mean, na.rm = TRUE)

# get the extent of this particular raster, and crop it accordingly
plotObs_future2_2 <- plotObs_future2 %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )

map_ecoRegion_future2 <- 
  ggplot() +
geom_raster(data = plotObs_future2_2, aes(x = x, 
                   y = y, 
                   fill = as.character(mean)), maxcell = 5e+20) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
  scale_fill_manual(name = "", 
                     values = c("tan", "darkgreen"), 
                     labels = c("Non-Forest", "Forest"),
                    na.translate = FALSE) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Forest classification predictions \nusing future climate data from the \nIPSL-CM5A-MR model"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     ) 
         

## Plot the maps together
  ggarrange(map_ecoRegion_current, map_ecoRegion_future1, map_ecoRegion_future2, nrow = 3) #%>% 
  #annotate_figure(fig.lab = "Model Predictions of Forest Classification with Contemporary and Forecasted Climate Data", fig.lab.size = 20, 
                 # )
  
rm(plotObs_future1, plotObs_future1_2, plotObs_future2, plotObs_future2_2, map_ecoRegion_current, map_ecoRegion_future1, map_ecoRegion_future2)
gc()
```


Below is a map of the 'scaling factor' that we use to make predictions of total tree cover CONUS-wide. If the model-predicted probability of forest is high or low, then we take the predictions of total tree cover for forest and non-forest at face value. However, if the model predicted forest probability is near the threshold for making the predictions binary, we interpolate the continuous P(Forest) predictions within this buffer to generate a continuous "scaling factor" that we use to mix the predictions of total tree cover from the forest and non-forest model together.

```{r, fig.width = 10}
# figure of transformed P(Forest) 
plotObs <- preds_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_contNew", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )

map_ecoRegion_current_Transformed<- ggplot() +
geom_spatraster(data = plotObs_2, maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
 scale_fill_gradientn(
                                             colors = c("tan", "orange", "purple", "forestgreen"),
                                             values = c(0, 1e-10, 0.99999, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Scaling factor for tree cover, based on contemporary \ndayMet data (2023)"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     ) 
         

## future 1
# figure of transformed P(Forest) 
plotObs <- preds_future1_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_contNew", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )


map_ecoRegion_future1_Transformed<- ggplot() +
geom_spatraster(data = plotObs_2, maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
 scale_fill_gradientn(
                                             colors = c("tan", "orange", "purple", "forestgreen"),
                                             values = c(0, 1e-10, 0.99999, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Scaling factor for tree cover, based on future climate data from the \nBNU-ESM model"),
     subtitle = c("Values are the average binary predictions \nwithin each 12 km x 12 km grid cell"), x = "", y = ""
     ) 

## future 2
# figure of transformed P(Forest) 
plotObs <- preds_future2_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "pred_contNew", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )


map_ecoRegion_future2_Transformed<- ggplot() +
geom_spatraster(data = plotObs_2, maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +
 scale_fill_gradientn(
                                             colors = c("tan", "orange", "purple", "forestgreen"),
                                             values = c(0, 1e-10, 0.99999, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Scaling factor for tree cover, based on future climate data from the \nIPSL-CM5A-MR model"),
     subtitle = c("Values are the average binary predictions \nwithin each 12 km x 12 km grid cell"), x = "", y = ""
     ) 

(map_ecoRegion_current_Transformed)
(map_ecoRegion_future1_Transformed)
(map_ecoRegion_future2_Transformed)

rm(plotObs, plotObs_2, map_ecoRegion_current_Transformed, map_ecoRegion_future1_Transformed, map_ecoRegion_future2_Transformed)
gc()
```

Now, we show the predictions from the model that predicts whether a location has *no* trees. These predictions are combined with the predictions from the forest model, such that only locations that the forest model predicts to be un-forested can then be predicted to be un-treed.


```{r, fig.width = 10}
# read in the model object
# we use the oneSELambda model, since the predictions are very similar to the best lambda model
zeroTreeMod <- readRDS("./models/yesOrNoTrees_oneSELambdaGLM_10.rds")
# get threshold for making model predictions binary
zeroTreeMod_threshold <- readRDS("./models/YesNoTreeModelPrevalence_secondBest.rds")

# predict w/ contemporary data
preds_byHand <- preds_byHand %>% 
  mutate(zeroTree_pred = predict(object = zeroTreeMod, climDatPred, type = "response"),
         zeroTree_binary = zeroTree_pred) %>% 
  # make predictions of zero tree binary
  mutate(zeroTree_binary = replace(zeroTree_binary, zeroTree_binary > zeroTreeMod_threshold, 1), 
         zeroTree_binary = replace(zeroTree_binary, zeroTree_binary <= zeroTreeMod_threshold, 0)) %>% 
  # now, get predictions for locations that the forest model predicts to be forested
  mutate(zeroTree_nonForest = zeroTree_binary) %>% 
  mutate(zeroTree_nonForest = replace(zeroTree_nonForest, pred_binary == 1, NA)) %>% 
  mutate(zeroTree_cont_nonForest = zeroTree_pred) %>% 
  mutate(zeroTree_cont_nonForest = replace(zeroTree_cont_nonForest, pred_binary == 1, NA))

# predict w/ future 1 data
preds_future1_byHand <- preds_future1_byHand %>% 
  mutate(zeroTree_pred = predict(object = zeroTreeMod, forecastClimSoilsDatPred_1, type = "response"),
         zeroTree_binary = zeroTree_pred) %>% 
  mutate(zeroTree_binary = replace(zeroTree_binary, zeroTree_binary > zeroTreeMod_threshold, 1), 
         zeroTree_binary = replace(zeroTree_binary, zeroTree_binary <= zeroTreeMod_threshold, 0)) %>% 
  # now, get predictions for locations that the forest model predicts to be forested
  mutate(zeroTree_nonForest = zeroTree_binary) %>% 
  mutate(zeroTree_nonForest = replace(zeroTree_nonForest, pred_binary == 1, NA)) %>% 
  mutate(zeroTree_cont_nonForest = zeroTree_pred) %>% 
  mutate(zeroTree_cont_nonForest = replace(zeroTree_cont_nonForest, pred_binary == 1, NA))

# predict w/ future 2 data
preds_future2_byHand <- preds_future2_byHand %>% 
  mutate(zeroTree_pred = predict(object = zeroTreeMod, forecastClimSoilsDatPred_2, type = "response"),
         zeroTree_binary = zeroTree_pred) %>% 
  mutate(zeroTree_binary = replace(zeroTree_binary, zeroTree_binary > zeroTreeMod_threshold, 1), 
         zeroTree_binary = replace(zeroTree_binary, zeroTree_binary <= zeroTreeMod_threshold, 0)) %>% 
  # now, get predictions for locations that the forest model predicts to be forested
  mutate(zeroTree_nonForest = zeroTree_binary) %>% 
  mutate(zeroTree_nonForest = replace(zeroTree_nonForest, pred_binary == 1, NA)) %>% 
  mutate(zeroTree_cont_nonForest = zeroTree_pred) %>% 
  mutate(zeroTree_cont_nonForest = replace(zeroTree_cont_nonForest, pred_binary == 1, NA))

## Binary predictions
# figure of transformed P(Forest) 
plotObs <- preds_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "zeroTree_nonForest", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )

map_zeroTreePred_current <- ggplot() +
geom_raster(data = plotObs_2, aes(x = x, y = y, fill = as.character(mean)),maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +  scale_fill_manual(name = "", 
                     values = c("wheat", "tan"), 
                     labels = c("No trees", "Trees"),
                    na.translate = FALSE) +
 # scale_fill_gradientn(
 #                                             colors = c("chocolate", "wheat"),
 #                                             values = c(0, 1),
 #                                             limits = c(0,1),
 #                                             na.value = "lightgrey"
 #                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Locations with no trees (binary), based on the zero tree model,\n--using contemporary dayMet data (2023)"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     ) 
         

## future 1
# figure of transformed P(Forest) 
plotObs <- preds_future1_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "zeroTree_nonForest", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )


map_zeroTreePred_future1 <- ggplot() +
geom_raster(data = plotObs_2, aes(x = x, y = y, fill = as.character(mean)),maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +  scale_fill_manual(name = "", 
                     values = c("wheat", "tan"), 
                     labels = c("No trees", "Trees"),
                    na.translate = FALSE) +
 # scale_fill_gradientn(
 #                                             colors = c("chocolate", "wheat"),
 #                                             values = c(0, 1),
 #                                             limits = c(0,1),
 #                                             na.value = "lightgrey"
 #                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Locations with no trees (binary), based on the zero tree model,\n--using future climate data from the BNU-ESM model"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     ) 

## future 2
# figure of transformed P(Forest) 
plotObs <- preds_future2_byHand %>% 
         #drop_na(paste(response)) %>% 
  #slice_sample(n = 5e4) %>%
  terra::vect(#geom = c("Lon", "Lat")
    ) %>% 
  terra::set.crs(crs(test_rastTemp)) %>% 
  terra::rasterize(y = test_rastTemp, 
                   field = "zeroTree_nonForest", 
                   fun = mean, na.rm = TRUE) 
# get the extent of this particular raster, and crop≠ it accordingly
plotObs_2 <- plotObs %>% 
  crop(ext(min(tempExt[,1]), max(tempExt[,1]),
           min(tempExt[,2]), max(tempExt[,2])) 
       )


map_zeroTreePred_future2 <- ggplot() +
geom_raster(data = plotObs_2, aes(x = x, y = y, fill = as.character(mean)),maxcell = 5e+10) + 
  geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>% st_crop(st_bbox(plotObs_2)) ,fill=NA ) +
 xlim(-2150000, 2510000) +
 ylim(-2005000, 1000000) +
  coord_sf(datum = st_crs(test_rastTemp)) +  scale_fill_manual(name = "", 
                     values = c("wheat", "tan"), 
                     labels = c("No trees", "Trees"),
                    na.translate = FALSE) +
 # scale_fill_gradientn(
 #                                             colors = c("chocolate", "wheat"),
 #                                             values = c(0, 1),
 #                                             limits = c(0,1),
 #                                             na.value = "lightgrey"
 #                                             ) +
             theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank()) + 
labs(title = c("Locations with no trees (binary), based on the zero tree model,\n--using future climate data from the IPSL-CM5A-MR model"),
     subtitle = c("Values are the average binary predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = ""
     ) 

(map_zeroTreePred_current)
#(map_zeroTreePred_current_cont)
(map_zeroTreePred_future1)
#(map_zeroTreePred_future1_cont)
(map_zeroTreePred_future2)
#(map_zeroTreePred_future2_cont)
rm(plotObs, plotObs_2, map_zeroTreePred_current, map_zeroTreePred_future1, map_zeroTreePred_future2)
gc()
```

 # Now, we generate CONUS-wide model predictions of *absolute* cover

First, we use our models to predict absolute cover at the ecoregion or CONUS scale, depending on the functional type, using both contemporary climate and weather data, and then with future climate and weather data downscaled from the two GCMs. For some functional types we were able to fit a model that worked well across all of CONUS, while for other types, models performed better when we fit them to a single ecoregion at a time. Below is a list of the models we use:

* total herbaceous cover in CONUS
* total tree cover in forested locations
* total tree cover in the un-forested locations
* total shrub cover in CONUS
* total bare ground cover in CONUS
* The proportion of total tree cover that is needle-leaved in CONUS
* The proportion of total tree cover that is broad-leaved in CONUS
* The proportion of total herbaceous cover that is forbs in CONUS
* The proportion of total herbaceous cover that is C3 grass in CONUS
* The proportion of total herbaceous cover that is C4 grass in CONUS

*Note: These model predictions do not incorporate input values for weather anomalies, so if a model includes a coefficient for a weather anomaly, that term is zeroed out in the predictions that are shown. This is for two reasons: First, we are interested in the overall predictions of the models, rather than in their variability from year to year (although we explore this in a subsequent section of this document). Second, we do not have weather anomaly data for all of CONUS on the dayMet scale due to computational constraints.
*Note 2: Complete equations for each model used here can be found in the document "03_CompareModels.html"
```{r}
# Total herbaceous cover:
# Grass/shrub: 1SE lambda model
# totalHerb_GS <-  readRDS("./models/betaLASSO/TotalHerbaceousCover_noTrees_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_oneSELambdaGLM.rds")
# # Forest: best lambda model
# totalHerb_F <-  readRDS("./models/betaLASSO/TotalHerbaceousCover_trees_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")
totalHerb_CONUS <-  readRDS("./models/betaLASSO/TotalHerbaceousCover_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")

# Total tree cover:
# Grass/shrub: best lambda model
totalTree_GS <- # include anomalies option
  readRDS("./models/betaLASSO/TotalTreeCover_transformed_noTrees_noTLP_FALSE_removeAnomaliesTRUE_trimAnom_bestLambdaGLM_observeDatForestClassification.rds")
#   #remove anomalies option #
#   readRDS("./models/betaLASSO/TotalTreeCover_trees_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")
# Forest: best lambda model
totalTree_F <- readRDS("./models/betaLASSO/TotalTreeCover_trees_noTLP_FALSE_removeAnomaliesTRUE_trimAnom_oneSELambdaGLM_observeDatForestClassification.rds")

# Bare ground:
# CONUS - 1SE lambda model
bareGround_CONUS <-  readRDS("./models/betaLASSO/BareGroundCover_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_halfSELambdaGLM.rds")

# Shrub:
# CONUS - best lambda model
shrub_CONUS <- readRDS("./models/betaLASSO/ShrubCover_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")

# Broad-leaved tree:
# Grass/shrub: best lambda model
# BLtree_GS <- readRDS("./models/betaLASSO/AngioTreeCover_prop_shrubGrass_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")
# Forest:  best lambda model
BLtree_CONUS <- readRDS("./models/betaLASSO/AngioTreeCover_prop_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_halfSELambdaGLM.rds")

# Needle-leaved tree:
# Grass/shrub: best lambda model
# NLtree_GS <-  readRDS("./models/betaLASSO/ConifTreeCover_prop_shrubGrass_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")
# Forest:  best lambda model
NLtree_CONUS <- readRDS("./models/betaLASSO/ConifTreeCover_prop_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_halfSELambdaGLM.rds")

# Forb:
# CONUS: best lambda model
forb_CONUS <-  readRDS("./models/betaLASSO/ForbCover_prop_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")

# C3 grass:
# CONUS: 1SE lambda model
C3grass_CONUS <- readRDS("./models/betaLASSO/C3GramCover_prop_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")

# C4 grass:
# CONUS: best lambda model
C4grass_CONUS <- readRDS("./models/betaLASSO/C4GramCover_prop_CONUS_noTLP_FALSE_removeAnomaliesFALSE_trimAnom_bestLambdaGLM.rds")

## Now, predict absolute cover with scaled climate/weather/soils data
## with contemporary climate data

# totalHerb_F_predsContemp <- makePredictions(predictionDF = climDatPred,
#                                             modelObject = totalHerb_F) %>%
#   rename(absTotalHerb_F = "modelPreds")
totalHerb_CONUS_predsContemp <- makePredictions(predictionDF = climDatPred,
                                            modelObject = totalHerb_CONUS, scale = FALSE) %>%
  rename(absTotalHerb_CONUS = "modelPreds")

totalTree_F_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = totalTree_F , scale = FALSE) %>%
  rename(absTotalTree_F = "modelPreds")

totalTree_GS_predsContemp <- makePredictions(predictionDF = climDatPred,
                                             modelObject = totalTree_GS, scale = FALSE) %>%
   rename(absTotalTree_GS = "modelPreds")
# back-transform the total tree in GS predictions to be between 0 and 0.1, and make all values below the cutoff 0
# read in zero cutoff
#zeroCutoff <- readRDS("./models/TotalTreeCoverInNonForest_zeroCutoff.rds")
totalTree_GS_predsContemp <- totalTree_GS_predsContemp %>%
  mutate(absTotalTree_GS = absTotalTree_GS/10) #%>%
  #mutate(absTotalTree_GS = replace(absTotalTree_GS, absTotalTree_GS <=zeroCutoff, 0 ))

bareGround_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = bareGround_CONUS, scale = FALSE) %>%
  rename(absBareGround_CONUS = "modelPreds")

shrub_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = shrub_CONUS, scale = FALSE) %>%
  rename(absShrub_CONUS = "modelPreds")

BLtree_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = BLtree_CONUS, scale = FALSE) %>%
  rename(percBLTree_CONUS = "modelPreds")

# BLtree_GS_predsContemp<- climDatPred %>%
#   mutate(percBLTree_GS = 0)
  # makePredictions(predictionDF = climDatPred,
  #                                           modelObject = BLtree_GS) %>%
  # rename(percBLTree_GS = "modelPreds")

NLtree_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = NLtree_CONUS, scale = FALSE) %>%
  rename(percNLTree_CONUS = "modelPreds")
# NLtree_GS_predsContemp <- climDatPred %>%
#   mutate(percNLTree_GS = 0)
  # makePredictions(predictionDF = climDatPred,
  #                                           modelObject = NLtree_GS) %>%
  # rename(percNLTree_GS = "modelPreds")

C3grass_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = C3grass_CONUS, scale = FALSE) %>%
  rename(percC3grass_CONUS = "modelPreds")

C4grass_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = C4grass_CONUS, scale = FALSE) %>%
  rename(percC4grass_CONUS = "modelPreds")

forb_CONUS_predsContemp<- makePredictions(predictionDF = climDatPred,
                                            modelObject = forb_CONUS, scale = FALSE) %>%
  rename(percForb_CONUS = "modelPreds")

contempPreds <- totalHerb_CONUS_predsContemp  %>%
  #cbind(totalHerb_CONUS_predsContemp %>% select(absTotalHerb_CONUS)) %>%
  cbind(totalTree_F_predsContemp %>% select(absTotalTree_F)) %>%
  cbind(totalTree_GS_predsContemp %>% select(absTotalTree_GS)) %>%
  cbind(bareGround_CONUS_predsContemp %>% select(absBareGround_CONUS)) %>%
  cbind(shrub_CONUS_predsContemp %>% select(absShrub_CONUS)) %>%
  cbind(BLtree_CONUS_predsContemp %>% select(percBLTree_CONUS)) %>%
  #cbind(BLtree_GS_predsContemp %>% select(percBLTree_GS)) %>%
  cbind(NLtree_CONUS_predsContemp %>% select(percNLTree_CONUS)) %>%
  #cbind(NLtree_GS_predsContemp %>% select(percNLTree_GS)) %>%
  cbind(C3grass_CONUS_predsContemp %>% select(percC3grass_CONUS)) %>%
  cbind(C4grass_CONUS_predsContemp %>% select(percC4grass_CONUS)) %>%
  cbind(forb_CONUS_predsContemp %>% select(percForb_CONUS)) %>%
  cbind(preds_byHand %>% select(pred_binary, pred_contNew, zeroTree_nonForest)) %>%
  rename(prob_Forest_b = "pred_binary",
         prob_Forest_c = "pred_contNew") %>%
  mutate(prob_grassShrub_b = 1 - prob_Forest_b,
         prob_grassShrub_c = 1 - prob_Forest_c) %>%
  select(-c(geometry.1:geometry.10))

# get predictions with first climate model
## with contemporary climate data
totalHerb_CONUS_predsFuture1 <- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                                modelObject = totalHerb_CONUS, scale = FALSE)  %>%
  rename(absTotalHerb_CONUS = "modelPreds")
#totalHerb_GS_predsFuture1 <- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
 #                                           modelObject = totalHerb_GS) %>%
#  rename(absTotalHerb_GS = "modelPreds")

totalTree_F_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = totalTree_F, scale = FALSE) %>%
  rename(absTotalTree_F = "modelPreds")

totalTree_GS_predsFuture1 <- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                             modelObject = totalTree_GS, scale = FALSE) %>%
   rename(absTotalTree_GS = "modelPreds")
# back-transform the total tree in GS predictions to be between 0 and 0.1, and make all values below the cutoff 0
totalTree_GS_predsFuture1 <- totalTree_GS_predsFuture1 %>%
  mutate(absTotalTree_GS = absTotalTree_GS/10) #%>%
  #mutate(absTotalTree_GS = replace(absTotalTree_GS, absTotalTree_GS <=zeroCutoff, 0 ))

bareGround_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = bareGround_CONUS, scale = FALSE) %>%
  rename(absBareGround_CONUS = "modelPreds")

shrub_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = shrub_CONUS, scale = FALSE) %>%
  rename(absShrub_CONUS = "modelPreds")

BLtree_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = BLtree_CONUS, scale = FALSE) %>%
  rename(percBLTree_CONUS = "modelPreds")
# BLtree_GS_predsFuture1 <- forecastClimSoilsDatPred_1 %>%
#   mutate(percBLTree_GS = 0)
  # makePredictions(predictionDF = forecastClimSoilsDatPred_1,
  #                                           modelObject = BLtree_GS) %>%
  # rename(percBLTree_GS = "modelPreds")

NLtree_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = NLtree_CONUS, scale = FALSE) %>%
  rename(percNLTree_CONUS = "modelPreds")
# NLtree_GS_predsFuture1 <- forecastClimSoilsDatPred_1 %>%
#   mutate(percNLTree_GS = 0)
  # makePredictions(predictionDF = forecastClimSoilsDatPred_1,
  #                                           modelObject = NLtree_GS) %>%
  # rename(percNLTree_GS = "modelPreds")

C3grass_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = C3grass_CONUS, scale = FALSE) %>%
  rename(percC3grass_CONUS = "modelPreds")

C4grass_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = C4grass_CONUS, scale = FALSE) %>%
  rename(percC4grass_CONUS = "modelPreds")

forb_CONUS_predsFuture1<- makePredictions(predictionDF = forecastClimSoilsDatPred_1,
                                            modelObject = forb_CONUS, scale = FALSE) %>%
  rename(percForb_CONUS = "modelPreds")


predsFuture1 <- totalHerb_CONUS_predsFuture1  %>%
  #cbind(totalHerb_GS_predsFuture1 %>% select(absTotalHerb_GS)) %>%
  cbind(totalTree_F_predsFuture1 %>% select(absTotalTree_F)) %>%
  cbind(totalTree_GS_predsFuture1 %>% select(absTotalTree_GS)) %>%
  cbind(bareGround_CONUS_predsFuture1 %>% select(absBareGround_CONUS)) %>%
  cbind(shrub_CONUS_predsFuture1 %>% select(absShrub_CONUS)) %>%
  cbind(BLtree_CONUS_predsFuture1 %>% select(percBLTree_CONUS)) %>%
  #cbind(BLtree_GS_predsFuture1 %>% select(percBLTree_GS)) %>%
  cbind(NLtree_CONUS_predsFuture1 %>% select(percNLTree_CONUS)) %>%
  #cbind(NLtree_GS_predsFuture1 %>% select(percNLTree_GS)) %>%
  cbind(C3grass_CONUS_predsFuture1 %>% select(percC3grass_CONUS)) %>%
  cbind(C4grass_CONUS_predsFuture1 %>% select(percC4grass_CONUS)) %>%
  cbind(forb_CONUS_predsFuture1 %>% select(percForb_CONUS)) %>%
  cbind(preds_future1_byHand %>% select(pred_binary, pred_contNew, zeroTree_nonForest)) %>%
  rename(prob_Forest_b = "pred_binary",
         prob_Forest_c = "pred_contNew") %>%
  mutate(prob_grassShrub_b = 1 - prob_Forest_b,
         prob_grassShrub_c = 1 - prob_Forest_c)

# get predictions with second climate model
totalHerb_CONUS_predsFuture2 <- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = totalHerb_CONUS, scale = FALSE) %>%
  rename(absTotalHerb_CONUS = "modelPreds")
# totalHerb_GS_predsFuture2 <- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
#                                             modelObject = totalHerb_GS) %>%
#   rename(absTotalHerb_GS = "modelPreds")

totalTree_F_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = totalTree_F, scale = FALSE) %>%
  rename(absTotalTree_F = "modelPreds")

totalTree_GS_predsFuture2 <- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                             modelObject = totalTree_GS, scale = FALSE) %>%
   rename(absTotalTree_GS = "modelPreds")
# back-transform the total tree in GS predictions to be between 0 and 0.1, and make all values below the cutoff 0
totalTree_GS_predsFuture2 <- totalTree_GS_predsFuture2 %>%
  mutate(absTotalTree_GS = absTotalTree_GS/10) #%>%
 # mutate(absTotalTree_GS = replace(absTotalTree_GS, absTotalTree_GS <=zeroCutoff, 0 ))

bareGround_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = bareGround_CONUS, scale = FALSE) %>%
  rename(absBareGround_CONUS = "modelPreds")

shrub_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = shrub_CONUS, scale = FALSE) %>%
  rename(absShrub_CONUS = "modelPreds")

BLtree_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = BLtree_CONUS, scale = FALSE) %>%
  rename(percBLTree_CONUS = "modelPreds")
# BLtree_GS_predsFuture2 <- forecastClimSoilsDatPred_2 %>%
#   mutate(percBLTree_GS = 0)
  # makePredictions(predictionDF = forecastClimSoilsDatPred_2,
  #                                           modelObject = BLtree_GS) %>%
  # rename(percBLTree_GS = "modelPreds")

NLtree_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = NLtree_CONUS, scale = FALSE) %>%
  rename(percNLTree_CONUS = "modelPreds")
# NLtree_GS_predsFuture2 <- forecastClimSoilsDatPred_2 %>%
#   mutate(percNLTree_GS = 0)
  # makePredictions(predictionDF = forecastClimSoilsDatPred_2,
  #                                           modelObject = NLtree_GS) %>%
  # rename(percNLTree_GS = "modelPreds")

C3grass_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = C3grass_CONUS, scale = FALSE) %>%
  rename(percC3grass_CONUS = "modelPreds")

C4grass_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = C4grass_CONUS, scale = FALSE) %>%
  rename(percC4grass_CONUS = "modelPreds")

forb_CONUS_predsFuture2<- makePredictions(predictionDF = forecastClimSoilsDatPred_2,
                                            modelObject = forb_CONUS, scale = FALSE) %>%
  rename(percForb_CONUS = "modelPreds")

predsFuture2 <- totalHerb_CONUS_predsFuture2  %>%
  #cbind(totalHerb_GS_predsFuture2 %>% select(absTotalHerb_GS)) %>%
  cbind(totalTree_F_predsFuture2 %>% select(absTotalTree_F)) %>%
  cbind(totalTree_GS_predsFuture2 %>% select(absTotalTree_GS)) %>%
  cbind(bareGround_CONUS_predsFuture2 %>% select(absBareGround_CONUS)) %>%
  cbind(shrub_CONUS_predsFuture2 %>% select(absShrub_CONUS)) %>%
  cbind(BLtree_CONUS_predsFuture2 %>% select(percBLTree_CONUS)) %>%
  #cbind(BLtree_GS_predsFuture2 %>% select(percBLTree_GS)) %>%
  cbind(NLtree_CONUS_predsFuture2 %>% select(percNLTree_CONUS)) %>%
  #cbind(NLtree_GS_predsFuture2 %>% select(percNLTree_GS)) %>%
  cbind(C3grass_CONUS_predsFuture2 %>% select(percC3grass_CONUS)) %>%
  cbind(C4grass_CONUS_predsFuture2 %>% select(percC4grass_CONUS)) %>%
  cbind(forb_CONUS_predsFuture2 %>% select(percForb_CONUS)) %>%
  cbind(preds_future2_byHand %>% select(pred_binary, pred_contNew, zeroTree_nonForest)) %>%
  rename(prob_Forest_b = "pred_binary",
         prob_Forest_c = "pred_contNew") %>%
  mutate(prob_grassShrub_b = 1 - prob_Forest_b,
         prob_grassShrub_c = 1 - prob_Forest_c)

rm(preds_byHand, preds_future1_byHand, preds_future2_byHand, 
   totalHerb_CONUS_predsContemp , totalTree_F_predsContemp, totalTree_GS_predsContemp, bareGround_CONUS_predsContemp, shrub_CONUS_predsContemp, BLtree_CONUS_predsContemp, NLtree_CONUS_predsContemp, C3grass_CONUS_predsContemp, C4grass_CONUS_predsContemp, forb_CONUS_predsContemp,
    totalHerb_CONUS_predsFuture1 , totalTree_F_predsFuture1, totalTree_GS_predsFuture1, bareGround_CONUS_predsFuture1, shrub_CONUS_predsFuture1, BLtree_CONUS_predsFuture1, NLtree_CONUS_predsFuture1, C3grass_CONUS_predsFuture1, C4grass_CONUS_predsFuture1, forb_CONUS_predsFuture1,
    totalHerb_CONUS_predsFuture2 , totalTree_F_predsFuture2, totalTree_GS_predsFuture2, bareGround_CONUS_predsFuture2, shrub_CONUS_predsFuture2, BLtree_CONUS_predsFuture2, NLtree_CONUS_predsFuture2, C3grass_CONUS_predsFuture2, C4grass_CONUS_predsFuture2, forb_CONUS_predsFuture2)
gc()
```

Once we generate predictions using each of these models, we transform the ecoregion-level predictions for total herbaceous cover and total tree cover into CONUS-wide predictions using the following steps.

1) First, we scale them according to predicted ecoregion probability from the forest classification model (for total tree cover model only).
We retain the ecoregion-level predictions for later use in calculating CONUS-wide predictions of 'level 2' functional types

e.g. absolute total tree cover in CONUS = (absolute total tree cover in grass/shrub * P(grass/shrub)) + (absolute total tree cover in forest * P(forest))

2) Then, for those models that are predicting proportions, we scale all of the relevant predictions so they sum to one. If models are ecoregion-level, we do this process at the ecoregion scale.
(e.g. Model predictions of the proportion of total herbaceous cover that is forbs, the proportion of total herbaceous cover that is C3 grass, and the proportion of total herbaceous cover that is C4 grass should sum to 1, so: scaled proportion of total herbaceous cover that is forbs = proportion of total herbaceous cover that is forbs/(proportion of total herbaceous cover that is forbs + proportion of total herbaceous cover that is C3 grass + proportion of total herbaceous cover that is C4 grass))

3) Then, we convert these predicted proportions into predictions of absolute cover. For the functional types that are predicted at the ecoregion level, we do this process for each ecoregion.
(e.g.  absolute cover of needle-leaved trees in grass/shrub = scaled proportion of total tree cover that is needle-leaved in grass/shrub * absolute cover of total trees in grass/shrub)

4) Finally, for those functional types that are predicted at the ecoregion level, we scale them to be CONUS-wide.

```{r}
#### write a function for scaling the predictions
makePredsCONUSwide <- function(dat) {
  # Step 1: calculate GS and F tree cover by region
  # 'prob_grassShrub_c' is the 'scaling factor' for un-forested locations, and 'prob-grassShrub-f' is the same but for forests
  # this step is taking the weighted mean of tree cover, CONUS-wide
  dat2 <- dat %>%
  mutate(absTotalTree_GS_scaled = prob_grassShrub_c * absTotalTree_GS,
         absTotalTree_F_scaled = prob_Forest_c * absTotalTree_F) %>%
    mutate(absTotalTree_CONUS = absTotalTree_GS_scaled + absTotalTree_F_scaled) 
    
  # Step 2: for all locations that are predicted to have zero tree cover (and are in the non-forest), make tree cover zero
    # prob_Forest_b = 'binary probability of forest'
    # zeroTree_nonForest has predictions of zero tree cover (already made binary), ONLY for locations that are model-predicted to be unforested
  dat2[dat2$prob_Forest_b == 0 & !is.na(dat2$prob_Forest_b) & dat2$zeroTree_nonForest == 0  & !is.na(dat2$zeroTree_nonForest), "absTotalTree_GS_scaled"] <- 0
  dat2[dat2$prob_Forest_b == 0 & !is.na(dat2$prob_Forest_b) & dat2$zeroTree_nonForest == 0  & !is.na(dat2$zeroTree_nonForest), "absTotalTree_CONUS"] <- 0
  
  # Step 3: scaling all 'second tier' cover group predictions of proportion to sum to 1
dat2 <- dat2 %>%
  mutate(percBLTree_scaled_CONUS = percBLTree_CONUS/(percBLTree_CONUS + percNLTree_CONUS),
          percNLTree_scaled_CONUS = percNLTree_CONUS/(percBLTree_CONUS + percNLTree_CONUS),
      percC3grass_scaled_CONUS = percC3grass_CONUS/(percC3grass_CONUS + percC4grass_CONUS + percForb_CONUS),
         percC4grass_scaled_CONUS = percC4grass_CONUS/(percC3grass_CONUS + percC4grass_CONUS + percForb_CONUS),
         percForb_scaled_CONUS = percForb_CONUS/(percC3grass_CONUS + percC4grass_CONUS + percForb_CONUS))

# Step 4: translate the 'second tier' cover groups percentages to absolute cover 
dat2 <- dat2 %>%
    mutate(absNLTree_CONUS = (percNLTree_scaled_CONUS * absTotalTree_CONUS),
         absBLTree_CONUS = (percBLTree_scaled_CONUS * absTotalTree_CONUS),
         absC3grass_CONUS = (percC3grass_scaled_CONUS * absTotalHerb_CONUS), # can use CONUS-wide total herbaceous since C3 grass isn't ecoregion-level
         absC4grass_CONUS = (percC4grass_scaled_CONUS * absTotalHerb_CONUS), # can use CONUS-wide total herbaceous since C4 grass isn't ecoregion-level
         absForb_CONUS = (percForb_scaled_CONUS * absTotalHerb_CONUS) # can use CONUS-wide total herbaceous since forb grass isn't ecoregion-level
  )

# just to retain names for the workflow (just a legacy for the workflow to run, not actually doing anything)
dat2 <- dat2 %>%
  mutate(absBLTree_CONUS_scaled = absBLTree_CONUS,
         absNLTree_CONUS_scaled = absNLTree_CONUS)

  return(dat2)
}

## for contemporary data
contempPreds <- makePredsCONUSwide(contempPreds)

## for future data model 1
predsFuture1 <- makePredsCONUSwide(predsFuture1)

## for future data model 2
predsFuture2 <- makePredsCONUSwide(predsFuture2)
```

Below are maps of predicted absolute cover for each functional group we modeled, after predictions have been scaled using the process outlined above.
These maps show model-predicted cover using contemporary climate and weather data, residuals when comparing these contemporary predictions to observations, and changes in model predictions of cover when predictions are made using simulated future climate and weather data from two GCMs.
```{r}
# prep observed data for use in figures
obsDat <- modDat_1_s %>%
  select(x, y, Year, ShrubCover, TotalHerbaceousCover, TotalTreeCover, BareGroundCover, C3GramCover_prop, C4GramCover_prop, ForbCover_prop, AngioTreeCover_prop, ConifTreeCover_prop) %>%
  mutate(absNLTree_CONUS = ConifTreeCover_prop * TotalTreeCover,
         absBLTree_CONUS = AngioTreeCover_prop * TotalTreeCover,
         absC3grass_CONUS = C3GramCover_prop * TotalHerbaceousCover,
         absC4grass_CONUS = C4GramCover_prop * TotalHerbaceousCover,
         absForb_CONUS = ForbCover_prop * TotalHerbaceousCover
         ) %>%
  rename(absShrub_CONUS = ShrubCover,
         absTotalTree_CONUS = TotalTreeCover,
         absTotalHerb_CONUS = TotalHerbaceousCover,
         absBareGround_CONUS = BareGroundCover)

# list of absolute cover names
responseNames <- c("absTotalTree_CONUS",
                   "absTotalHerb_CONUS", "absShrub_CONUS", "absBareGround_CONUS", "absNLTree_CONUS", "absBLTree_CONUS", "absC3grass_CONUS", "absC4grass_CONUS", "absForb_CONUS")

absoluteCoverMaps_contemp <- lapply(responseNames,
                                      FUN = function(x) {
                                       ## contemporary absolute cover
                                        # rasterize response
                                        resp_rast <- contempPreds %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_2 <- resp_rast %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        # make a map
                                        resp_map <- ggplot() +
                                          geom_spatraster(data = resp_rast_2, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Predictions of absolute cover of \n",x),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"),
                                                         x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                           scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank())

  # add an inset showing a density plot
           # get data
           data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
             contempPreds %>%
                st_drop_geometry() %>%
             select(x) %>%
            pivot_longer(c(x), names_to = "lyr", values_to = "Cover")

           # make insets
           inset_plots <- data_inset %>%
             split(f = .$lyr) %>%
             purrr::map(~annotation_custom2(
    grob = ggplotGrob(get_inset(.) #+
                        #scale_y_continuous(limits=c(0,105), breaks = c(0, 50, 100))
                      ),
    data = data.frame(lyr=unique(.$lyr
      )),
    ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
  )

         resp_map <- resp_map + inset_plots


                                        ## calculate residuals
                                        # make maps of residuals between absolute cover predictions and observations
                                        # rasterize observations
                                        plotObservations <- obsDat %>%
                                          terra::vect(geom = c("x", "y")) %>%
                                          terra::set.crs(crs(crs_veg)) %>%
                                          terra::project(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                         plotObservations <- plotObservations %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        # calculate residuals (observed - predicted)
                                        resids_rast <- plotObservations - resp_rast_2
                                        ## aggregate the residuals raster to make it easier to see
                                        resids_rastCoarse <- resids_rast %>%
                                          aggregate(fact = 8, fun = "mean", na.rm = TRUE)
                                        # map the residuals
                                        mapResids <- ggplot() +
                                          geom_spatraster(data = resids_rastCoarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resids_rastCoarse)) ,fill=NA ) +
                                                    labs(title = paste0("Residuals (observations - predicted absolute cover) \nfor ",x),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("red", "orange", "white", "green", "blue"),
                                                                #values = c(-1, -.5, 0, 0.5, 1), 
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          
                                           # scale_fill_gradient2(low = "red",
                                           #                     mid = "white" ,
                                           #                     high = "blue" ,
                                           #                     midpoint = 0,   na.value = "lightgrey",
                                           #                     limits = c(-1,1) )+
                                                                 theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )
                                        # add an inset showing a density plot
           # get data
           data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
            data.frame("resids" = terra::values(resids_rast$mean, na.rm = TRUE, mat = FALSE)) %>%
             select(resids) %>%
            pivot_longer(c(resids), names_to = "lyr", values_to = "Cover")

           # make insets
           inset_plots <- data_inset %>%
             split(f = .$lyr) %>%
             purrr::map(~annotation_custom2(
    grob = ggplotGrob(get_inset(.) #+
                        #scale_y_continuous(limits=c(0,105), breaks = c(0, 50, 100))
                      ),
    data = data.frame(lyr=unique(.$lyr
      )),
    ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
  )

         mapResids <- mapResids + inset_plots
                                        
  #                                       # make maps of % CHANGE residuals between absolute cover predictions and observations
  #                                       # rasterize observations
  #                                       # calculate residuals (observed - predicted)/obs
  #                                       resids_rast_CHANGE <- (plotObservations - resp_rast_2)/plotObservations
  #                                       ## aggregate the residuals raster to make it easier to see
  #                                       resids_rast_CHANGE_Coarse <- resids_rast_CHANGE %>%
  #                                         aggregate(fact = 8, fun = "mean", na.rm = TRUE)
  #                                       # map the residuals
  #                                       mapResids_CHANGE <- ggplot() +
  #                                         geom_spatraster(data = resids_rast_CHANGE_Coarse, maxcell = 50000000 ) +
  #                                         geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
  #                                                   st_crop(st_bbox(resids_rast_CHANGE_Coarse)) ,fill=NA ) +
  #                                                   labs(title = paste0("Relative Residuals \n(observations - predicted absolute cover)/observations \nfor ",x),
  #                                                        subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
  #                                         xlim(-2150000, 2510000) +
  #                                         ylim(-2005000, 1000000) +
  #                                         coord_sf(datum = st_crs(test_rastTemp)) +
  #                                          scale_fill_gradient2(low = "red",
  #                                                              mid = "white" ,
  #                                                              high = "blue" ,
  #                                                              midpoint = 0,   na.value = "lightgrey")+
  #                                                                theme(axis.text = element_blank(),
  #                                                                      strip.text = element_text(size = 15),
  #                                                                      title = element_text(size = 18),
  #                                                                      axis.ticks = element_blank()
  #                                                                      )
  #                                       # add an inset showing a density plot
  #          # get data
  #          data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
  #           data.frame("resids" = terra::values(resids_rast_CHANGE$mean, na.rm = TRUE, mat = FALSE)) %>%
  #            select(resids) %>%
  #           pivot_longer(c(resids), names_to = "lyr", values_to = "Cover")
  # 
  #          # make insets
  #          inset_plots <- data_inset %>%
  #            split(f = .$lyr) %>%
  #            purrr::map(~annotation_custom2(
  #   grob = ggplotGrob(get_inset(.) #+
  #                       #scale_y_continuous(limits=c(0,105), breaks = c(0, 50, 100))
  #                     ),
  #   data = data.frame(lyr=unique(.$lyr
  #     )),
  #   ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
  # )
  # 
  #        mapResids_CHANGE <- mapResids_CHANGE + inset_plots
  # 
  # 

                                         ## future model 1 absolute cover
                                        # rasterize response
                                        resp_rast_future1 <- predsFuture1 %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_future1_2 <- resp_rast_future1 %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        resp_rast_future1_coarse <- resp_rast_future1_2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        # calculate ABSOLUTE differences between contemporary preds and future 1 preds (deltas)
                                        rast_deltas_future1 <- resp_rast_future1_2 - resp_rast_2
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future1_coarse <- rast_deltas_future1 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        
                                        ## calculate RELATIVE differences between contemporary preds and future 1 preds (deltas) (future - current)/current
                                        rast_deltas_future1_REL <- (resp_rast_future1_2 - resp_rast_2)/(resp_rast_2+.000001)
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future1_REL_coarse <- rast_deltas_future1_REL %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)

                                        # make a map of the actual predicted values for modeled climate 1
                                        resp_Actual_map_future1 <- ggplot() +
                                          geom_spatraster(data = resp_rast_future1_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_future1_coarse)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n - based on data from climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
                                          predsFuture1 %>%
                                          dplyr::select(all_of(x)) %>%
                                          pivot_longer(c(x), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- data_inset %>%
                                          split(f = .$lyr) %>%
                                          purrr::map(~annotation_custom2(
                                            grob = ggplotGrob(get_inset(.)
                                                              ),
                                            data = data.frame(lyr=unique(.$lyr
                                                                         )),
                                            ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                            )

                                        resp_Actual_map_future1 <- resp_Actual_map_future1 + inset_plots

                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future1 <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future1_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(rast_deltas_future1_coarse)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("#f89540", "#cc4778", "white","#7e03a8", "#0d0887"),
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          # scale_fill_gradient2(low = "orange",
                                          #                      mid = "white" ,
                                          #                      high = "purple" ,
                                          #                      midpoint = 0, limits = c(-1,1),  na.value = "lightgrey")  +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future1$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- data_inset %>%
                                          split(f = .$lyr) %>%
                                          purrr::map(~annotation_custom2(
                                            grob = ggplotGrob(get_inset(.)
                                                              ),
                                            data = data.frame(lyr=unique(.$lyr
                                                                         )),
                                            ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                            )

                                        resp_delta_map_future1 <- resp_delta_map_future1 + inset_plots
                                        
                                        # ## RELATIVE deltas for model 1
                                        # # make a map of the deltas of predicted values for modeled climate 1
                                        # resp_delta_map_future1_REL <- ggplot() +
                                        #   geom_spatraster(data = rast_deltas_future1_REL_coarse, maxcell = 50000000 ) +
                                        #   geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                        #             st_crop(st_bbox(rast_deltas_future1_REL_coarse)) ,fill=NA ) +
                                        #             labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                        #                  subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                        #   xlim(-2150000, 2510000) +
                                        #   ylim(-2005000, 1000000) +
                                        #   coord_sf(datum = st_crs(test_rastTemp)) +
                                        #   # scale_fill_gradientn(colors = c("#f89540",  "white", "#0d0887"),
                                        #   #                       #breaks = c(-1, -.5, 0, 0.5, 1), 
                                        #   #                      #limits = c(-1,1),
                                        #   #                       na.value = "lightgrey") + 
                                        #   scale_fill_gradient2(low = "#f89540",
                                        #                        mid = "white" ,
                                        #                        high = "#0d0887" ,
                                        #                        midpoint = 0, #limits = c(-1,1),  
                                        #                        na.value = "lightgrey")  +
                                        #   theme(axis.text = element_blank(),
                                        #                                strip.text = element_text(size = 15),
                                        #                                title = element_text(size = 18),
                                        #                                axis.ticks = element_blank()
                                        #                                )
                                        # 
                                        # # add an inset showing a density plot
                                        # # get data
                                        # data_inset <- data.frame("deltas" = terra::values(rast_deltas_future1_REL$mean, na.rm = TRUE, mat = FALSE)) %>%
                                        #   pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # # make insets
                                        # inset_plots <- data_inset %>%
                                        #   split(f = .$lyr) %>%
                                        #   purrr::map(~annotation_custom2(
                                        #     grob = ggplotGrob(get_inset(.)
                                        #                       ),
                                        #     data = data.frame(lyr=unique(.$lyr
                                        #                                  )),
                                        #     ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                        #     )
                                        # 
                                        # resp_delta_map_future1_REL <- resp_delta_map_future1_REL + inset_plots
                                        
                                        ## future model 2 absolute cover
                                        # rasterize response
                                        resp_rast_future2 <- predsFuture2 %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_future2_2 <- resp_rast_future2 %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        resp_rast_future2_coarse <- resp_rast_future2_2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        # calculate differences between contemporary preds and future 1 preds (deltas)
                                        rast_deltas_future2 <- resp_rast_future2_2 - resp_rast_2
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future2_coarse <- rast_deltas_future2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)

                                          ## calculate RELATIVE differences between contemporary preds and future 1 preds (deltas) (future - current)/current
                                        rast_deltas_future2_REL <- (resp_rast_future2_2 - resp_rast_2)/(resp_rast_2+.000001)
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future2_REL_coarse <- rast_deltas_future2_REL %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        
                                        # make a map of the actual predicted values for modeled climate 1
                                        resp_Actual_map_future2 <- ggplot() +
                                          geom_spatraster(data = resp_rast_future2_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_future2_coarse)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n - based on data from climate model: IPSL-CM5A-MR (France)"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
                                          predsFuture2 %>%
                                          dplyr::select(all_of(x)) %>%
                                          pivot_longer(c(x), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- data_inset %>%
                                          split(f = .$lyr) %>%
                                          purrr::map(~annotation_custom2(
                                            grob = ggplotGrob(get_inset(.)
                                                              ),
                                            data = data.frame(lyr=unique(.$lyr
                                                                         )),
                                            ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                            )

                                        resp_Actual_map_future2 <- resp_Actual_map_future2 + inset_plots

                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future2 <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future2_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(rast_deltas_future2_coarse)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: IPSL-CM5A-MR (France)"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("#f89540", "#cc4778", "white","#7e03a8", "#0d0887"),
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future2$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- data_inset %>%
                                          split(f = .$lyr) %>%
                                          purrr::map(~annotation_custom2(
                                            grob = ggplotGrob(get_inset(.)
                                                              ),
                                            data = data.frame(lyr=unique(.$lyr
                                                                         )),
                                            ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                            )

                                        resp_delta_map_future2 <- resp_delta_map_future2 + inset_plots

 ## RELATIVE deltas for model 1
                                        # # make a map of the deltas of predicted values for modeled climate 1
                                        # resp_delta_map_future2_REL <- ggplot() +
                                        #   geom_spatraster(data = rast_deltas_future2_REL_coarse, maxcell = 50000000 ) +
                                        #   geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                        #             st_crop(st_bbox(rast_deltas_future2_REL_coarse)) ,fill=NA ) +
                                        #             labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                        #                  subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                        #   xlim(-2150000, 2510000) +
                                        #   ylim(-2005000, 1000000) +
                                        #   coord_sf(datum = st_crs(test_rastTemp)) +
                                        #   # scale_fill_gradientn(colors = c("#f89540",  "white", "#0d0887"),
                                        #   #                       #breaks = c(-1, -.5, 0, 0.5, 1), 
                                        #   #                      #limits = c(-1,1),
                                        #   #                       na.value = "lightgrey") + 
                                        #   scale_fill_gradient2(low = "#f89540",
                                        #                        mid = "white" ,
                                        #                        high = "#0d0887" ,
                                        #                        midpoint = 0, #limits = c(-1,1),  
                                        #                        na.value = "lightgrey")  +
                                        #   theme(axis.text = element_blank(),
                                        #                                strip.text = element_text(size = 15),
                                        #                                title = element_text(size = 18),
                                        #                                axis.ticks = element_blank()
                                        #                                )
                                        # 
                                        # # add an inset showing a density plot
                                        # # get data
                                        # data_inset <- data.frame("deltas" = terra::values(rast_deltas_future2_REL$mean, na.rm = TRUE, mat = FALSE)) %>%
                                        #   pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # # make insets
                                        # inset_plots <- data_inset %>%
                                        #   split(f = .$lyr) %>%
                                        #   purrr::map(~annotation_custom2(
                                        #     grob = ggplotGrob(get_inset(.)
                                        #                       ),
                                        #     data = data.frame(lyr=unique(.$lyr
                                        #                                  )),
                                        #     ymin = -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000)
                                        #     )
                                        # 
                                        # resp_delta_map_future2_REL <- resp_delta_map_future2_REL + inset_plots
                                        
                                        return(list("rast" = resp_rast_2,
                                                    "map" = resp_map,
                                                    
                                                    "rast_resids" = resids_rast,
                                                    "map_resids" = mapResids,
                                                    
                                                    #"rast_resids_relative" = resids_rast_CHANGE,
                                                    #"map_resids_relative" = mapResids_CHANGE,
                                                    
                                                    "rast_future1" = resp_rast_future1_2,
                                                    "map_future1" = resp_Actual_map_future1,
                                                    "map_future1_deltas" = resp_delta_map_future1,
                                                    #"map_future1_deltas_relative" = resp_delta_map_future1_REL,
                                                    
                                                    "rast_future2" = resp_rast_future2_2,
                                                    "map_future2" = resp_Actual_map_future2,
                                                    "map_future2_deltas" = resp_delta_map_future2#,
                                                    #"map_future2_deltas_relative" = resp_delta_map_future2_REL
                                                    ))
                                      })


names(absoluteCoverMaps_contemp) <- c("absTotalTree_CONUS",
                                      "absTotalHerb_CONUS", "absShrub_CONUS", "absBareGround_CONUS", "absNLTree_CONUS", "absBLTree_CONUS", "absC3grass_CONUS", "absC4grass_CONUS", "absForb_CONUS")
```

```{r, eval = TRUE}
## save the rasterized absolute cover values for contemporary data
for(i in 1:length(responseNames)) {
  if ( i == 1) {
    outRast <- absoluteCoverMaps_contemp[[responseNames[i]]]$rast
  } else {
    outRast <- c(outRast, absoluteCoverMaps_contemp[[responseNames[i]]]$rast)
  }
}
names(outRast) <- responseNames

## save for later use
terra::writeRaster(outRast, filename = "../../../Data_processed/CoverData/finalCoverData/finalAbsoluteCoverData_2023dayMetClimateData.tif", overwrite = TRUE)
```

```{r  fig.height=20, fig.width=18}
library(patchwork)
(absoluteCoverMaps_contemp$absTotalTree_CONUS$map + absoluteCoverMaps_contemp$absTotalTree_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absTotalTree_CONUS$map_future1 + absoluteCoverMaps_contemp$absTotalTree_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absTotalTree_CONUS$map_future2+ absoluteCoverMaps_contemp$absTotalTree_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}
(absoluteCoverMaps_contemp$absTotalHerb_CONUS$map + absoluteCoverMaps_contemp$absTotalHerb_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absTotalHerb_CONUS$map_future1 + absoluteCoverMaps_contemp$absTotalHerb_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absTotalHerb_CONUS$map_future2+ absoluteCoverMaps_contemp$absTotalHerb_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absShrub_CONUS$map + absoluteCoverMaps_contemp$absShrub_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absShrub_CONUS$map_future1 + absoluteCoverMaps_contemp$absShrub_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absShrub_CONUS$map_future2+ absoluteCoverMaps_contemp$absShrub_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absBareGround_CONUS$map + absoluteCoverMaps_contemp$absBareGround_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absBareGround_CONUS$map_future1 + absoluteCoverMaps_contemp$absBareGround_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absBareGround_CONUS$map_future2+ absoluteCoverMaps_contemp$absBareGround_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absNLTree_CONUS$map + absoluteCoverMaps_contemp$absNLTree_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absNLTree_CONUS$map_future1 + absoluteCoverMaps_contemp$absNLTree_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absNLTree_CONUS$map_future2+ absoluteCoverMaps_contemp$absNLTree_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absBLTree_CONUS$map + absoluteCoverMaps_contemp$absBLTree_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absBLTree_CONUS$map_future1 + absoluteCoverMaps_contemp$absBLTree_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absBLTree_CONUS$map_future2+ absoluteCoverMaps_contemp$absBLTree_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absC3grass_CONUS$map + absoluteCoverMaps_contemp$absC3grass_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absC3grass_CONUS$map_future1 + absoluteCoverMaps_contemp$absC3grass_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absC3grass_CONUS$map_future2+ absoluteCoverMaps_contemp$absC3grass_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absC4grass_CONUS$map + absoluteCoverMaps_contemp$absC4grass_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absC4grass_CONUS$map_future1 + absoluteCoverMaps_contemp$absC4grass_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absC4grass_CONUS$map_future2+ absoluteCoverMaps_contemp$absC4grass_CONUS$map_future2_deltas)
```
--- 
```{r  fig.height=20, fig.width=18}

(absoluteCoverMaps_contemp$absForb_CONUS$map + absoluteCoverMaps_contemp$absForb_CONUS$map_resids)/
 (absoluteCoverMaps_contemp$absForb_CONUS$map_future1 + absoluteCoverMaps_contemp$absForb_CONUS$map_future1_deltas)/
  (absoluteCoverMaps_contemp$absForb_CONUS$map_future2+ absoluteCoverMaps_contemp$absForb_CONUS$map_future2_deltas)

rm(absoluteCoverMaps_contemp)
gc()
```

Now, we show quantile plots for  model predictions of absolute cover made with contemporary data. These plots compare final, scaled predictions of absolute cover to observed absolute cover. However, instead of showing raw data, which can be uninformative with large datasets such as ours, we show the average model predicted value or observed value within a given quantile of a given predictor variable. In this case, we are showing values within each percentile. We also show the inter-quantile range (IQR) of model-predicted and response values in each percentile, which appear as bands behind the points showing the mean values. Note that the predictor variables included in each quantile plot are the predictors that appear in all of the models used to generate a given absolute cover prediction (e.g. the predictors variables included in the quantile plot for absolute needle-leaved tree cover are the predictors that are included in any o fthe following models: total tree cover-grass/shrub, total tree cover-forest, prop. needle-leaved tree cover-grass/shrub, and prop. needle-leaved cover-forest). Note also that the predictor variables in these figures are scaled.
```{r}
# predict forest classification
modDat_ecoregionFitQuantile <- modDat_1_s %>% 
  # update names (change the name of scaled variables to exclude _s
  dplyr::select(Year:BareGroundCover, x:y, tmin_s:AWHC_s)
names(modDat_ecoregionFitQuantile)[35:84] <- str_remove(names(modDat_ecoregionFitQuantile)[35:84], pattern = "_s$")
  

# predict with contemporary climate data
preds_byHand_quantile <-  makePredictions(modelObject = forestMod, predictionDF = modDat_ecoregionFitQuantile, scale = TRUE) %>% 
  rename( "pred" = "modelPreds" )
#    preds_byHand_quantile <-   
#    modDat_ecoregionFitQuantile %>%
#    mutate(pred2 =  as.numeric(1/ (1 + exp(-(3.38565522 + -0.79822221*((tmean - 10.6678425) / 5.14823079) +  4.74536062*((prcp - 767.94900623) / 506.45362111) + -0.49798680*((prcpTempCorr - -0.02007952) / 0.40677333) +  0.22745494*((isothermality - 36.51862827) / 5.41402375) + -0.39051271*((clay - 17.45502795) / 8.23327615) +  0.53857137*((coarse - 11.18397843) / 11.24234211) + -0.40059849*((AWHC - 14.91029339) / 5.4856826) + -0.83406990*I(((prcp - 767.94900623) / 506.45362111)^2) + -0.94850342*I(((prcpTempCorr - -0.02007952) / 0.40677333)^2) +  0.19450621*I(((isothermality - 36.51862827) / 5.41402375)^2) + -0.04772718*I(((clay - 17.45502795) / 8.23327615)^2) + -0.26519959*I(((coarse - 11.18397843) / 11.24234211)^2) +  0.07438930*I(((AWHC - 14.91029339) / 5.4856826)^2) + -0.29049829*((prcp - 767.94900623) / 506.45362111)*((isothermality - 36.51862827) / 5.41402375) + -0.16800569*((tmean - 10.6678425) / 5.14823079)*((isothermality - 36.51862827) / 5.41402375) +  0.07625545*((tmean - 10.6678425) / 5.14823079)*((prcp - 767.94900623) / 506.45362111) + -0.55536190*((tmean - 10.6678425) / 5.14823079)*((prcpTempCorr - -0.02007952) / 0.40677333) + -0.37720980*((clay - 17.45502795) / 8.23327615)*((AWHC - 14.91029339) / 5.4856826))))))
# # convert the continuous probability predictions to binary using previously-identified threshold  and interpolate

preds_byHand_quantile <- preds_byHand_quantile %>%
  mutate(pred_binary = pred,
    pred_binary = replace(pred_binary, pred_binary >best_threshold_bestLambda, 1), # now, make the interpolated continuous model predictions binary
         pred_binary = replace(pred_binary, pred_binary <= best_threshold_bestLambda, 0)) %>%
  mutate(pred_contNew = interpFun_forest(pred))

# fix attributes so we can predict
#attr(preds_byHand_quantile, "row.names") <- as.integer(attr(preds_byHand_quantile, "row.names"))
## now, predict for zero tree model
# predict w/ contemporary data
# preds_byHand_quantile <-  makePredictions(modelObject = zeroTreeMod, predictionDF = preds_byHand_quantile, scale = TRUE) %>% 
#   rename( "zeroTree_pred" = "modelPreds" )

## I can't get the code above to run , so for now this is done by hand, although I recognize that isn't optimal)
preds_byHand_quantile <- preds_byHand_quantile %>% 
  mutate(zeroTree_pred = as.numeric(1/ (1 + exp(-(
    2.958559 + (4.078250*prcp) +  
      (-0.520455*prcpTempCorr) + 
      (0.231447*isothermality) + 
      (-0.242756*clay) + 
      (0.211847*coarse) + 
      (-0.534807*AWHC) + 
      (-0.007554 * I(tmean^2)) + 
      (-0.791094 * I(prcp^2)) + 
      (-0.948347 *I(prcpTempCorr^2)) + 
      (0.515462 * I(isothermality^2)) +  
      (-0.026416 * I(clay^2)) + 
      (-0.299854 * clay*AWHC) + 
      (0.063700*clay*coarse))))))
         
  
 preds_byHand_quantile <- preds_byHand_quantile %>%  
  mutate(#zeroTree_pred = predict(object = zeroTreeMod, preds_byHand_quantile, type = "response"),
         zeroTree_binary = zeroTree_pred) %>% 
  # make predictions of zero tree binary
  mutate(zeroTree_binary = replace(zeroTree_binary, zeroTree_binary > zeroTreeMod_threshold, 1), 
         zeroTree_binary = replace(zeroTree_binary, zeroTree_binary <= zeroTreeMod_threshold, 0))

 
## for zero tree model, change so that predictions are only for unforested locations
 preds_byHand_quantile <- preds_byHand_quantile%>% 
  # now, get predictions for locations that the forest model predicts to be forested
  mutate(zeroTree_nonForest = zeroTree_binary) %>% 
  mutate(zeroTree_nonForest = replace(zeroTree_nonForest, pred_binary == 1, NA)) %>% 
  mutate(zeroTree_cont_nonForest = zeroTree_pred) %>% 
  mutate(zeroTree_cont_nonForest = replace(zeroTree_cont_nonForest, pred_binary == 1, NA))
 
## now, predict for each functional type and zero tree model
# rename the data.frame so that the scaled predictors have the correct names for the models to predict with
scaledPrednames <- modDat_1_s %>%
  select(tmin_s:AWHC_s) %>%
  names()
modDat_quantile <- modDat_1_s %>%
  select(ShrubCover, TotalHerbaceousCover, TotalTreeCover, C3GramCover_prop, C4GramCover_prop,
         ForbCover_prop, AngioTreeCover_prop, ConifTreeCover_prop, BareGroundCover, x, y, tmin_s:AWHC_s, newRegion) %>%
  rename_with(~str_remove(string = .x, pattern = "_s$"), any_of(scaledPrednames))


## with the observed data, convert proportions to absolute cover
# take away scaling attributes
for (var in colnames(modDat_quantile)) {
  attr(modDat_quantile[[deparse(as.name(var))]], "scaled:center") <- NULL
  attr(modDat_quantile[[deparse(as.name(var))]], "scaled:scale") <- NULL
}

modDat_quantile <- modDat_quantile %>%
  mutate(across(tmin:AWHC, ~ as.vector(.x)))

## with contemporary climate data
totalHerb_CONUS_predsQuantile <- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = totalHerb_CONUS, scale = FALSE) %>%
  rename(absTotalHerb_CONUS = "modelPreds")
# totalHerb_GS_predsQuantile <- makePredictions(predictionDF = modDat_quantile,
#                                             modelObject = totalHerb_GS, scale = FALSE) %>%
#   rename(absTotalHerb_GS = "modelPreds")

totalTree_F_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = totalTree_F, scale = FALSE) %>%
  rename(absTotalTree_F = "modelPreds")

totalTree_GS_predsQuantile <- makePredictions(predictionDF = modDat_quantile,
                                             modelObject = totalTree_GS, scale = FALSE) %>%
   rename(absTotalTree_GS = "modelPreds")
# back-transform the total tree in GS predictions to be between 0 and 0.1, and make all values below the cutoff 0
totalTree_GS_predsQuantile <- totalTree_GS_predsQuantile %>%
  mutate(absTotalTree_GS = absTotalTree_GS/10) #%>%
 # mutate(absTotalTree_GS = replace(absTotalTree_GS, absTotalTree_GS <=zeroCutoff, 0 ))

bareGround_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = bareGround_CONUS, scale = FALSE) %>%
  rename(absBareGround_CONUS = "modelPreds")

shrub_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = shrub_CONUS, scale = FALSE) %>%
  rename(absShrub_CONUS = "modelPreds")

BLtree_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = BLtree_CONUS, scale = FALSE) %>%
  rename(percBLTree_CONUS = "modelPreds")
# BLtree_GS_predsQuantile <- modDat_quantile %>%
#   mutate(percBLTree_GS = 0)#makePredictions(predictionDF = modDat_quantile,
                                            #modelObject = BLtree_GS, scale = FALSE) %>%
 # rename(percBLTree_GS = "modelPreds")

NLtree_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = NLtree_CONUS, scale = FALSE) %>%
  rename(percNLTree_CONUS = "modelPreds")
# NLtree_GS_predsQuantile <- modDat_quantile %>%
#   mutate(percNLTree_GS = 0)
  #makePredictions(predictionDF = modDat_quantile,
  #                                           modelObject = NLtree_GS, scale = FALSE) %>%
  # rename(percNLTree_GS = "modelPreds")

C3grass_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = C3grass_CONUS, scale = FALSE) %>%
  rename(percC3grass_CONUS = "modelPreds")

C4grass_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = C4grass_CONUS, scale = FALSE) %>%
  rename(percC4grass_CONUS = "modelPreds")

forb_CONUS_predsQuantile<- makePredictions(predictionDF = modDat_quantile,
                                            modelObject = forb_CONUS, scale = FALSE) %>%
  rename(percForb_CONUS = "modelPreds")

preds_quantile <- totalHerb_CONUS_predsQuantile  %>%
  #cbind(totalHerb_GS_predsQuantile %>% select(absTotalHerb_GS)) %>%
  cbind(totalTree_F_predsQuantile %>% select(absTotalTree_F)) %>%
  cbind(totalTree_GS_predsQuantile %>% select(absTotalTree_GS)) %>%
  cbind(bareGround_CONUS_predsQuantile %>% select(absBareGround_CONUS)) %>%
  cbind(shrub_CONUS_predsQuantile %>% select(absShrub_CONUS)) %>%
  #cbind(BLtree_F_predsQuantile %>% select(percBLTree_F)) %>%
  cbind(BLtree_CONUS_predsQuantile %>% select(percBLTree_CONUS)) %>%
  #cbind(NLtree_F_predsQuantile %>% select(percNLTree_F)) %>%
  cbind(NLtree_CONUS_predsQuantile %>% select(percNLTree_CONUS)) %>%
  cbind(C3grass_CONUS_predsQuantile %>% select(percC3grass_CONUS)) %>%
  cbind(C4grass_CONUS_predsQuantile %>% select(percC4grass_CONUS)) %>%
  cbind(forb_CONUS_predsQuantile %>% select(percForb_CONUS)) %>%
  cbind(preds_byHand_quantile %>% select(pred_binary, pred_contNew, zeroTree_binary)) %>%
  rename(prob_Forest_b = "pred_binary",
         prob_Forest_c = "pred_contNew") %>%
  mutate(prob_grassShrub_b = 1 - prob_Forest_b,
         prob_grassShrub_c = 1 - prob_Forest_c)

# now, scale and relativize
preds_quantile  <- makePredsCONUSwide(dat = preds_quantile)

# for observed data, scale proportions to absolute cover.
preds_quantile <- preds_quantile %>%

  mutate(C3GramCover_abs = C3GramCover_prop * TotalHerbaceousCover,
         C4GramCover_abs = C4GramCover_prop * TotalHerbaceousCover,
         ForbCover_abs = ForbCover_prop * TotalHerbaceousCover,
         NLTreeCover_abs = ConifTreeCover_prop * TotalTreeCover,
         BLTreeCover_abs = AngioTreeCover_prop * TotalTreeCover)

```


```{r}
# total trees
# predictions for absolute needle leaved tree cover
decilePredNames_totalTree<- c(str_remove_all(c(str_split(c(names(coefficients(totalTree_F)), names(coefficients(totalTree_GS))), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]"))  %>%
  unique() %>%
    str_remove(pattern = "ntercept\\)") %>%
  #str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("log")) %>%
    str_remove(pattern = "\\)+") %>%
    str_remove(pattern = "\\+") %>%
  str_remove(pattern = regex("[:digit:]+")) %>%
    str_remove(pattern = "\\^") %>%
    str_squish() %>%
  unique()
  )
decilePredNames_totalTree <- decilePredNames_totalTree[decilePredNames_totalTree != ""]
decilePredNames_totalTree <- decilePredNames_totalTree[decilePredNames_totalTree != "phi"]

deciles_totTree <- predvars2deciles(df = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# calculate deciles for total trees using dataset that has total tree, NL tree, and BL tree
deciles_totTree_NLBL <- predvars2deciles(df = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)


# NL trees
# predictions for absolute needle leaved tree cover
decilePredNames_NLTree<- c(str_remove_all(c(str_split(names(coefficients(NLtree_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))) %>%
  unique()
decilePredNames_NLTree <- decilePredNames_NLTree[decilePredNames_NLTree != ""]
deciles_NLtree <- predvars2deciles(df = preds_quantile %>% select(NLTreeCover_abs, absNLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na()
                                   , response_vars = c("NLTreeCover_abs", "absNLTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_NLTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )


# # BL trees
# unique(c(names(coefficients(totalTree_F)), names(coefficients(totalTree_GS)), names(coefficients(BLtree_GS)), names(coefficients(BLtree_F))))
decilePredNames_BLTree<- c(str_remove_all(c(str_split(names(coefficients(BLtree_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))) %>%
  unique()
decilePredNames_BLTree <- decilePredNames_BLTree[decilePredNames_BLTree != ""]

deciles_BLtree <- predvars2deciles(df = preds_quantile %>% select("BLTreeCover_abs", "absBLTree_CONUS",
                                                                  decilePredNames_BLTree) %>% drop_na(),
response_vars = c("BLTreeCover_abs", "absBLTree_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_BLTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# shrubs
# unique(c(names(coefficients(shrub_CONUS))))
decilePredNames_shrub <- c(str_remove_all(c(str_split(names(coefficients(shrub_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))) %>%
  unique()
decilePredNames_shrub <- decilePredNames_shrub[decilePredNames_shrub != ""]

deciles_shrub <- predvars2deciles(df = preds_quantile %>% select("ShrubCover", "absShrub_CONUS",
                                                                 decilePredNames_shrub) %>% drop_na(),
                                  response_vars = c("ShrubCover", "absShrub_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_shrub), # for predictors, combine list of predictors for all models
cut_points = seq(0, 1, 0.01)
                                     )

# bare ground
# unique(c(names(coefficients())))
decilePredNames_bareGround <- c(str_remove_all(c(str_split(names(coefficients(bareGround_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_bareGround <- decilePredNames_bareGround[decilePredNames_bareGround != ""]
deciles_bareGround <- predvars2deciles(df = preds_quantile %>% select("BareGroundCover", "absBareGround_CONUS",
                                                                      decilePredNames_bareGround
                                                                      ) %>% drop_na(),
                                       response_vars = c("BareGroundCover", "absBareGround_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_bareGround), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )
# total herbaceous cover
#unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS))))
decilePredNames_totalHerb <- c(str_remove_all(c(str_split(names(coefficients(totalHerb_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_totalHerb <- decilePredNames_totalHerb[decilePredNames_totalHerb != ""]
deciles_totalHerb <- predvars2deciles(df = preds_quantile %>% select("TotalHerbaceousCover", "absTotalHerb_CONUS", decilePredNames_totalHerb) %>% drop_na(),                response_vars = c("TotalHerbaceousCover", "absTotalHerb_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_totalHerb), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# forbs
# unique(c(names(coefficients(totalTree_F)), names(coefficients(totalTree_GS)), names(coefficients(BLtree_GS)), names(coefficients(BLtree_F))))
decilePredNames_forb <- c(str_remove_all(c(str_split(names(coefficients(forb_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_forb <- decilePredNames_forb[decilePredNames_forb != ""]
deciles_forb <- predvars2deciles(df = preds_quantile %>% select("ForbCover_abs", "absForb_CONUS",
                                                                decilePredNames_forb) %>%
                                   drop_na(),
                                 response_vars = c("ForbCover_abs", "absForb_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_forb), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# C3 grass
# unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS)), names(coefficients(C3grass_CONUS))))
decilePredNames_C3Grass <- c(str_remove_all(c(str_split(names(coefficients(C3grass_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_C3Grass <- decilePredNames_C3Grass[decilePredNames_C3Grass != ""]
deciles_C3grass <- predvars2deciles(df = preds_quantile %>% select("C3GramCover_abs", "absC3grass_CONUS",
                                                                   decilePredNames_C3Grass) %>%
                                      drop_na(),
                                    response_vars = c("C3GramCover_abs", "absC3grass_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_C3Grass), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )
# C4 grass
# unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS)), names(coefficients(C3grass_CONUS))))
decilePredNames_C4Grass <- c(str_remove_all(c(str_split(names(coefficients(C4grass_CONUS)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_C4Grass <- decilePredNames_C4Grass[decilePredNames_C4Grass != ""]
deciles_C4grass <- predvars2deciles(df = preds_quantile %>% select("C4GramCover_abs", "absC4grass_CONUS",
                                                                   decilePredNames_C4Grass) %>%
                                      drop_na(),
                                    response_vars = c("C4GramCover_abs", "absC4grass_CONUS"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_C4Grass), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

## decile plot for data w/ all tree cover types (i.e. complete observations) using function (not by hand)
quantPlot_totalTrees_NLBL <- decile_dotplot_pq(df = deciles_totTree_NLBL, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover, CONUS-wide (only complete tree observations): Quantile Plot")

quantPlot_totalTrees_NLBL <- add_dotplot_inset(quantPlot_totalTrees_NLBL,
                                               df = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                               response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                               dfRaw = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                               add_smooth = TRUE, deciles = FALSE)
# make a density plot
 quantPlot_totalTrees_NLBL <- ggarrange(quantPlot_totalTrees_NLBL, ggplot( preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

## quantile plot for total tree w/ all available data in forest regions - model-defined
deciles_totTree_Forest <- predvars2deciles(df = preds_quantile %>%
                                             filter(prob_Forest_b == 1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_F <- decile_dotplot_pq(df = deciles_totTree_Forest, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Model-Defined Forest (all available data): Quantile Plot")

quantPlot_totalTrees_F <- add_dotplot_inset(quantPlot_totalTrees_F,
                                            df = preds_quantile %>%
                                             filter(prob_Forest_b == 1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na() ,
                                            response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                            dfRaw = preds_quantile %>%
                                             filter(prob_Forest_b == 1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                            add_smooth = TRUE, deciles = FALSE)
# make a density plot
 quantPlot_totalTrees_F <- ggarrange(quantPlot_totalTrees_F, ggplot(preds_quantile  %>%
                                             filter(prob_Forest_b == 1)
                                             %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                             #filter(TotalTreeCover > 0.1)
                                             ) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

## quantile plot for total tree w/ all available data in non-forest regions (using model classification of forest)
deciles_totTree_nonForest <- predvars2deciles(df = preds_quantile %>%
                                             filter(prob_Forest_b == 0) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_noF <- decile_dotplot_pq(df = deciles_totTree_nonForest, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Model-Defined Non-Forest (all available data): Quantile Plot")

quantPlot_totalTrees_noF <- add_dotplot_inset(quantPlot_totalTrees_noF,
                                              df = preds_quantile %>%
                                             filter(prob_Forest_b == 0) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                             response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                             dfRaw = preds_quantile %>%
                                             filter(prob_Forest_b == 0) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                             add_smooth = TRUE, deciles = FALSE)

# add a density plot
 quantPlot_totalTrees_noF <- ggarrange(quantPlot_totalTrees_noF, ggplot( preds_quantile  %>%
                                             filter(prob_Forest_b == 0) %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                             ) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )


 ## quantile plot for total tree w/ all available data in forest regions (data-defined)
deciles_totTree_Forest_dataDef <- predvars2deciles(df = preds_quantile %>%
                                             filter(TotalTreeCover > .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_F_dd <- decile_dotplot_pq(df = deciles_totTree_Forest_dataDef, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Data-Defined Forest (all available data): Quantile Plot")

quantPlot_totalTrees_F_dd <- add_dotplot_inset(quantPlot_totalTrees_F_dd,
                                            df = preds_quantile %>%
                                             filter(TotalTreeCover > .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na() ,
                                            response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                            dfRaw = preds_quantile %>%
                                             filter(TotalTreeCover > .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                            add_smooth = TRUE, deciles = FALSE)
# make a density plot
 quantPlot_totalTrees_F_dd <- ggarrange(quantPlot_totalTrees_F_dd, ggplot(preds_quantile  %>%
                                             filter(TotalTreeCover > .1) %>%
                                               select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                             #filter(TotalTreeCover > 0.1)
                                             ) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

## quantile plot for total tree w/ all available data in non-forest regions (using data-defined classification of forest)
deciles_totTree_nonForest_dataDef <- predvars2deciles(df = preds_quantile %>%
                                             filter(TotalTreeCover <= .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_noF_dd <- decile_dotplot_pq(df = deciles_totTree_nonForest_dataDef, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Data-Defined  Non-Forest (all available data): Quantile Plot")

quantPlot_totalTrees_noF_dd <- add_dotplot_inset(quantPlot_totalTrees_noF_dd,
                                              df = preds_quantile %>%
                                             filter(TotalTreeCover <= .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                             response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                             dfRaw = preds_quantile %>%
                                             filter(TotalTreeCover <= .1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                             add_smooth = TRUE, deciles = FALSE)

# add a density plot
 quantPlot_totalTrees_noF_dd <- ggarrange(quantPlot_totalTrees_noF_dd, ggplot( preds_quantile  %>%
                                             filter(TotalTreeCover <= .1) %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                             ) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

## quantile plot for total tree w/ data for total tree, NL tree and BL tree in forest regions
deciles_totTree_Forest_NLBL <- predvars2deciles(df = preds_quantile %>%
                                             filter(prob_Forest_b == 1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_F_NLBL <- decile_dotplot_pq(df = deciles_totTree_Forest_NLBL, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Model-Defined Forest (all available data): Quantile Plot")

quantPlot_totalTrees_F_NLBL <- add_dotplot_inset(quantPlot_totalTrees_F_NLBL, df = preds_quantile %>%
                                             filter(prob_Forest_b == 1) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na() , response = c("TotalTreeCover", "absTotalTree_CONUS"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

## quantile plot for total tree w/ data for total tree, NL tree and BL tree in non-forest regions
deciles_totTree_nonForest_NLBL <- predvars2deciles(df = preds_quantile %>%
                                             filter(prob_Forest_b == 0) %>%
                                             select(TotalTreeCover, absTotalTree_CONUS,
                                                                        NLTreeCover_abs, absNLTree_CONUS,
                                                                        BLTreeCover_abs, absBLTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                   , response_vars = c("TotalTreeCover", "absTotalTree_CONUS"), # name of observations, followed by name of predictions
                                   pred_vars = c(decilePredNames_totalTree), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc NL tree for GS and F)
cut_points = seq(0, 1, 0.01)
)
quantPlot_totalTrees_noF_NLBL <- decile_dotplot_pq(df = deciles_totTree_nonForest_NLBL, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover in Non-Forest (all available data): Quantile Plot")

quantPlot_totalTrees_noF_NLBL <- add_dotplot_inset(quantPlot_totalTrees_noF_NLBL, df = deciles_totTree_nonForest_NLBL, response = c("TotalTreeCover", "absTotalTree_CONUS"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

```

```{r}
# total trees trees
# absolute cover
quantPlot_totalTrees <- decile_dotplot_pq(df = deciles_totTree, response= c("TotalTreeCover", "absTotalTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Tree Cover, CONUS-wide (all available data): Quantile Plot")

quantPlot_totalTrees <- add_dotplot_inset(quantPlot_totalTrees,
                                          df = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                          response = c("TotalTreeCover", "absTotalTree_CONUS"),
                                          dfRaw = preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na(),
                                          add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_totalTrees <- ggarrange(quantPlot_totalTrees, ggplot( preds_quantile %>% select(TotalTreeCover, absTotalTree_CONUS,
                                                                   decilePredNames_totalTree
                                                                   ) %>% drop_na()
                                             ) +
  geom_density(aes(TotalTreeCover)) +
  geom_density(aes(absTotalTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# needle-leaved trees
# absolute cover
quantPlot_NLtreeAbs <- decile_dotplot_pq(df = deciles_NLtree, response= c("NLTreeCover_abs", "absNLTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Needle-Leaved Tree Cover, CONUS-wide : Quantile Plot")

quantPlot_NLtreeAbs <- add_dotplot_inset(quantPlot_NLtreeAbs,
                                         df = preds_quantile %>% select(NLTreeCover_abs, absNLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na(),
                                         response = c("NLTreeCover_abs", "absNLTree_CONUS"),
                                         dfRaw = preds_quantile %>% select(NLTreeCover_abs, absNLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na(),
                                         add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_NLtreeAbs <- ggarrange(quantPlot_NLtreeAbs, ggplot(preds_quantile %>% select(NLTreeCover_abs, absNLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na()
                                             ) +
  geom_density(aes(NLTreeCover_abs)) +
  geom_density(aes(absNLTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# broad-leaved trees
# absolute cover
quantPlot_BLtreeAbs <- decile_dotplot_pq(df = deciles_BLtree, response= c("BLTreeCover_abs", "absBLTree_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Broad-Leaved Tree Cover, CONUS-wide : Quantile Plot")

quantPlot_BLtreeAbs <- add_dotplot_inset(quantPlot_BLtreeAbs,
                                         df = preds_quantile %>% select(BLTreeCover_abs, absBLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na(),
                                         response = c("BLTreeCover_abs", "absBLTree_CONUS"),
                                         dfRaw = preds_quantile %>% select(BLTreeCover_abs, absBLTree_CONUS,
                                                                  decilePredNames_NLTree) %>% drop_na(),
                                         add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_BLtreeAbs <- ggarrange(quantPlot_BLtreeAbs, ggplot(preds_quantile %>% select(BLTreeCover_abs, absBLTree_CONUS,
                                                                  decilePredNames_BLTree) %>% drop_na()
                                             ) +
  geom_density(aes(BLTreeCover_abs)) +
  geom_density(aes(absBLTree_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# shrubs
quantPlot_shrubs <- decile_dotplot_pq(df = deciles_shrub, response= c("ShrubCover", "absShrub_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Shrub Cover, CONUS-wide : Quantile Plot")

quantPlot_shrubs <- add_dotplot_inset(quantPlot_shrubs,
                                      df = preds_quantile %>% select(ShrubCover, absShrub_CONUS,
                                                                  decilePredNames_shrub) %>% drop_na(),
                                      response = c("ShrubCover", "absShrub_CONUS"),
                                      dfRaw = preds_quantile %>% select(ShrubCover, absShrub_CONUS,
                                                                  decilePredNames_shrub) %>% drop_na(),
                                      add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_shrubs <- ggarrange(quantPlot_shrubs, ggplot(preds_quantile %>% select(ShrubCover, absShrub_CONUS,
                                                                  decilePredNames_shrub) %>% drop_na()
                                             ) +
  geom_density(aes(ShrubCover)) +
  geom_density(aes(absShrub_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# bare ground
quantPlot_bareGround <- decile_dotplot_pq(df = deciles_bareGround, response= c("BareGroundCover", "absBareGround_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Bare Ground Cover, CONUS-wide : Quantile Plot")

quantPlot_bareGround <- add_dotplot_inset(quantPlot_bareGround,
                                          df = preds_quantile %>% select(BareGroundCover, absBareGround_CONUS,
                                                                  decilePredNames_bareGround) %>% drop_na(),
                                          response = c("BareGroundCover", "absBareGround_CONUS"),
                                          dfRaw = preds_quantile %>% select(BareGroundCover, absBareGround_CONUS,
                                                                  decilePredNames_bareGround) %>% drop_na(),
                                          add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_bareGround <- ggarrange(quantPlot_bareGround, ggplot(preds_quantile %>% select(BareGroundCover, absBareGround_CONUS,
                                                                  decilePredNames_bareGround) %>% drop_na()
                                             ) +
  geom_density(aes(BareGroundCover)) +
  geom_density(aes(absBareGround_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# total herbaceous
quantPlot_totHerb <- decile_dotplot_pq(df = deciles_totalHerb, response= c("TotalHerbaceousCover", "absTotalHerb_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Total Herbaceous Cover, CONUS-wide : Quantile Plot")

quantPlot_totHerb <- add_dotplot_inset(quantPlot_totHerb,
                                       df = preds_quantile %>% select(TotalHerbaceousCover, absTotalHerb_CONUS,
                                                                  decilePredNames_totalHerb) %>% drop_na(),
                                       response = c("TotalHerbaceousCover", "absTotalHerb_CONUS"),
                                       dfRaw = preds_quantile %>% select(TotalHerbaceousCover, absTotalHerb_CONUS,
                                                                  decilePredNames_totalHerb) %>% drop_na(),
                                       add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_totHerb <- ggarrange(quantPlot_totHerb, ggplot(preds_quantile %>% select(TotalHerbaceousCover, absTotalHerb_CONUS,
                                                                  decilePredNames_totalHerb) %>% drop_na()
                                             ) +
  geom_density(aes(TotalHerbaceousCover)) +
  geom_density(aes(absTotalHerb_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# forbs
quantPlot_forbs <- decile_dotplot_pq(df = deciles_forb, response= c("ForbCover_abs", "absForb_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute Forb Cover, CONUS-wide : Quantile Plot")

quantPlot_forbs <- add_dotplot_inset(quantPlot_forbs,
                                     df = preds_quantile %>% select(ForbCover_abs, absForb_CONUS,
                                                                  decilePredNames_forb) %>% drop_na(),
                                     response = c("ForbCover_abs", "absForb_CONUS"),
                                     dfRaw = preds_quantile %>% select(ForbCover_abs, absForb_CONUS,
                                                                  decilePredNames_forb) %>% drop_na(),
                                     add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_totHerb <- ggarrange(quantPlot_totHerb, ggplot(preds_quantile %>% select(ForbCover_abs, absForb_CONUS,
                                                                  decilePredNames_forb) %>% drop_na()
                                             ) +
  geom_density(aes(ForbCover_abs)) +
  geom_density(aes(absForb_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

# C3 grass
quantPlot_c3grass <- decile_dotplot_pq(df = deciles_C3grass, response= c("C3GramCover_abs", "absC3grass_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute C3 Grass Cover, CONUS-wide : Quantile Plot")

quantPlot_c3grass <- add_dotplot_inset(quantPlot_c3grass,
                                       df =  preds_quantile %>% select(C3GramCover_abs, absC3grass_CONUS,
                                                                  decilePredNames_C3Grass) %>% drop_na(),
                                       response = c("C3GramCover_abs", "absC3grass_CONUS"),
                                       dfRaw = preds_quantile %>% select(C3GramCover_abs, absC3grass_CONUS,
                                                                  decilePredNames_C3Grass) %>% drop_na(),
                                       add_smooth = TRUE, deciles = FALSE)
# add a density plot
 quantPlot_c3grass <- ggarrange(quantPlot_c3grass, ggplot( preds_quantile %>% select(C3GramCover_abs, absC3grass_CONUS,
                                                                  decilePredNames_C3Grass) %>% drop_na()
                                             ) +
  geom_density(aes(C3GramCover_abs)) +
  geom_density(aes(absC3grass_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )

#C4 grass
quantPlot_c4grass <- decile_dotplot_pq(df = deciles_C4grass, response= c("C4GramCover_abs", "absC4grass_CONUS"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Absolute C4 Grass Cover, CONUS-wide : Quantile Plot")

quantPlot_c4grass <- add_dotplot_inset(quantPlot_c4grass,
                                       df = preds_quantile %>% select(C4GramCover_abs, absC4grass_CONUS,
                                                                  decilePredNames_C4Grass) %>% drop_na(),
                                       response = c("C4GramCover_abs", "absC4grass_CONUS"),
                                       dfRaw = preds_quantile %>% select(C4GramCover_abs, absC4grass_CONUS,
                                                                  decilePredNames_C4Grass) %>% drop_na(),
                                       add_smooth = TRUE,
                                       deciles = FALSE)
# add a density plot
 quantPlot_c4grass <- ggarrange(quantPlot_c4grass, ggplot( preds_quantile %>% select(C4GramCover_abs, absC4grass_CONUS,
                                                                  decilePredNames_C4Grass) %>% drop_na()
                                             ) +
  geom_density(aes(C4GramCover_abs)) +
  geom_density(aes(absC4grass_CONUS), col = "lightblue") +
    ggtitle("Density of observed values (black) and predicted values (blue)"),
  ncol = 1, heights = c(.75, .25)
 )
```

### Below are quantile plots for total tree cover using all available tree cover data, for both all of CONUS, and then forested and unforested regions

```{r fig.height = 10, fig.width = 10, warning=FALSE, message=FALSE}
## now, display the figures
quantPlot_totalTrees
quantPlot_totalTrees_NLBL
quantPlot_totHerb
quantPlot_shrubs
quantPlot_bareGround
quantPlot_NLtreeAbs
quantPlot_BLtreeAbs
quantPlot_c3grass
quantPlot_c4grass
quantPlot_forbs
```

Below, we show the RMSE and bias of the final, scaled estimates of absolute cover for each functional type in comparison to the observed data.
```{r}
long_absPreds <- preds_quantile %>%
  mutate(uniqueID = 1:nrow(preds_quantile)) %>%
  select(uniqueID, absTotalHerb_CONUS, absTotalTree_CONUS, absShrub_CONUS, absBareGround_CONUS,
                        absNLTree_CONUS, absBLTree_CONUS, absC3grass_CONUS, absC4grass_CONUS, absForb_CONUS) %>%
  rename(TotalHerb = absTotalHerb_CONUS,
         TotalTree = absTotalTree_CONUS,
         Shrub = absShrub_CONUS,
         BareGround = absBareGround_CONUS,
         NLTree = absNLTree_CONUS,
         BLTree = absBLTree_CONUS,
         C3gram = absC3grass_CONUS,
         C4gram = absC4grass_CONUS,
         Forb = absForb_CONUS) %>%
  pivot_longer(cols = c(TotalHerb, TotalTree, Shrub, BareGround, NLTree, BLTree, C3gram, C4gram, Forb),
                names_to = "names",
               values_to = "preds_values"
               ) %>%
  select(uniqueID, names, preds_values)

long_absObs <- preds_quantile %>%
  mutate(uniqueID = 1:nrow(preds_quantile)) %>%
  select(uniqueID, TotalHerbaceousCover, TotalTreeCover, ShrubCover, BareGroundCover,
                        NLTreeCover_abs, BLTreeCover_abs, C3GramCover_abs, C4GramCover_abs, ForbCover_abs) %>%
  rename(TotalHerb = TotalHerbaceousCover,
         TotalTree = TotalTreeCover,
         Shrub = ShrubCover,
         BareGround = BareGroundCover,
         NLTree = NLTreeCover_abs,
         BLTree = BLTreeCover_abs,
         C3gram = C3GramCover_abs,
         C4gram = C4GramCover_abs,
         Forb = ForbCover_abs) %>%
  pivot_longer(cols = c(TotalHerb, TotalTree, Shrub, BareGround, NLTree, BLTree, C3gram, C4gram, Forb),
                names_to = "names",
               values_to = "obs_values"
               ) %>%
  select(uniqueID, names, obs_values)

longObsPreds <- long_absObs %>%
  left_join(long_absPreds)

## calculate bias
(summaryTableAbs <- longObsPreds %>%
  mutate(resids = obs_values - preds_values) %>%
  group_by(names) %>%
  dplyr::summarise(bias = mean(resids, na.rm = TRUE),
            RMSE = round(sqrt(mean(resids^2, na.rm = TRUE)),4)
            ) %>%
  slice(8, 9, 7, 2, 6, 1, 3, 4, 5) %>% kable(
  format = "html",
  col.names = c("Model Name", "Model bias mean(obs. - pred.)", "model RMSE")
))
```

Finally, we show how these model predictions made using contemporary climate and weather data change over time from 2010 to 2020.
```{r}
# get climate data from dayMet (d.f. is "climDatPred_unscaled")

preds_byHand_time <- 
  makePredictions(modelObject = forestMod, predictionDF = climDat_time_Pred, scale = TRUE) %>% 
  rename("pred" = "modelPreds")
  
  # mutate(pred =  as.numeric(1/ (1 + exp(-(3.38565522 + -0.79822221*((tmean - 10.6678425) / 5.14823079) +  4.74536062*((prcp - 767.94900623) / 506.45362111) + -0.49798680*((prcpTempCorr - -0.02007952) / 0.40677333) +  0.22745494*((isothermality - 36.51862827) / 5.41402375) + -0.39051271*((clay - 17.45502795) / 8.23327615) +  0.53857137*((coarse - 11.18397843) / 11.24234211) + -0.40059849*((AWHC - 14.91029339) / 5.4856826) + -0.83406990*I(((prcp - 767.94900623) / 506.45362111)^2) + -0.94850342*I(((prcpTempCorr - -0.02007952) / 0.40677333)^2) +  0.19450621*I(((isothermality - 36.51862827) / 5.41402375)^2) + -0.04772718*I(((clay - 17.45502795) / 8.23327615)^2) + -0.26519959*I(((coarse - 11.18397843) / 11.24234211)^2) +  0.07438930*I(((AWHC - 14.91029339) / 5.4856826)^2) + -0.29049829*((prcp - 767.94900623) / 506.45362111)*((isothermality - 36.51862827) / 5.41402375) + -0.16800569*((tmean - 10.6678425) / 5.14823079)*((isothermality - 36.51862827) / 5.41402375) +  0.07625545*((tmean - 10.6678425) / 5.14823079)*((prcp - 767.94900623) / 506.45362111) + -0.55536190*((tmean - 10.6678425) / 5.14823079)*((prcpTempCorr - -0.02007952) / 0.40677333) + -0.37720980*((clay - 17.45502795) / 8.23327615)*((AWHC - 14.91029339) / 5.4856826))))))
# convert the continuous probability predictions to binary using previously-identified threshold
preds_byHand_time <- preds_byHand_time %>%
  mutate(pred_binary = pred,
    pred_binary = replace(pred_binary, pred_binary >best_threshold_bestLambda, 1), # now, make the interpolated continuous model predictions binary
         pred_binary = replace(pred_binary, pred_binary <= best_threshold_bestLambda, 0)) %>%
  mutate(pred_contNew = interpFun_forest(pred))

## predict locations w/ zero tree cover
# predict w/ contemporary data
preds_byHand_time <- preds_byHand_time %>% 
  mutate(zeroTree_pred = predict(object = zeroTreeMod, climDat_time_Pred, type = "response"),
         zeroTree_binary = zeroTree_pred) %>% 
  # make predictions of zero tree binary
  mutate(zeroTree_binary = replace(zeroTree_binary, zeroTree_binary > zeroTreeMod_threshold, 1), 
         zeroTree_binary = replace(zeroTree_binary, zeroTree_binary <= zeroTreeMod_threshold, 0)) %>% 
  # now, get predictions for locations that the forest model predicts to be forested
  mutate(zeroTree_nonForest = zeroTree_binary) %>% 
  mutate(zeroTree_nonForest = replace(zeroTree_nonForest, pred_binary == 1, NA)) %>% 
  mutate(zeroTree_cont_nonForest = zeroTree_pred) %>% 
  mutate(zeroTree_cont_nonForest = replace(zeroTree_cont_nonForest, pred_binary == 1, NA))

# names(climDat_time)[c(5, 6, 7, 13, 10, 12, 99, 101, 102, 103)] <- c("T_warmestMonth_meanAnnAvg_CLIM", "T_coldestMonth_meanAnnAvg_CLIM", "precip_wettestMonth_meanAnnAvg_CLIM", "annWaterDeficit_meanAnnAvg_CLIM", "PrecipTempCorr_meanAnnAvg_CLIM", "isothermality_meanAnnAvg_CLIM", "soilDepth", "avgSandPerc_acrossDepth", "avgCoarsePerc_acrossDepth", "avgOrganicCarbonPerc_0_3cm")
#
# # predict with contemporary climate data
# preds_byHand_time <- climDat_time %>%
#   mutate(pred = 1/(1 + exp(-( 9.8726 + -0.2999*T_warmestMonth_meanAnnAvg_CLIM +  0.2456*T_coldestMonth_meanAnnAvg_CLIM +  0.0106*precip_wettestMonth_meanAnnAvg_CLIM + -0.0621*annWaterDeficit_meanAnnAvg_CLIM + -2.7863*PrecipTempCorr_meanAnnAvg_CLIM +  0.0540*isothermality_meanAnnAvg_CLIM + -0.0076*soilDepth +  0.0335*avgSandPerc_acrossDepth +  0.0310*avgCoarsePerc_acrossDepth +  0.2726*avgOrganicCarbonPerc_0_3cm))))
# #predictionsModel <- predict(object = forestMod, type = "response", newdata = climDatPred_unscaled)

## with contemporary climate data
totalHerb_CONUS_predsOverTime <- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = totalHerb_CONUS, scale = FALSE) %>%
  rename(absTotalHerb_CONUS = "modelPreds")
# totalHerb_GS_predsOverTime <-
#     makePredictions(predictionDF = climDat_time_Pred,
#                                             modelObject = totalHerb_GS) %>%
#   rename(absTotalHerb_GS = "modelPreds")

totalTree_F_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = totalTree_F, scale = FALSE) %>%
  rename(absTotalTree_F = "modelPreds")

totalTree_GS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred, modelObject = totalTree_GS, scale = FALSE) %>%
  rename(absTotalTree_GS = "modelPreds") %>%
  mutate(absTotalTree_GS = absTotalTree_GS/10) #%>%
  #mutate(absTotalTree_GS = replace(absTotalTree_GS, absTotalTree_GS <=zeroCutoff, 0 ))


bareGround_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = bareGround_CONUS, scale = FALSE) %>%
  rename(absBareGround_CONUS = "modelPreds")

shrub_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = shrub_CONUS, scale = FALSE) %>%
  rename(absShrub_CONUS = "modelPreds")

BLtree_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = BLtree_CONUS, scale = FALSE) %>%
  rename(percBLTree_CONUS = "modelPreds")
# BLtree_GS_predsOverTime<- climDat_time_Pred %>%
#   mutate(percBLTree_GS = 0)
  # makePredictions(predictionDF = climDat_time_Pred,
  #                                           modelObject = BLtree_GS) %>%
  # rename(percBLTree_GS = "modelPreds")

NLtree_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = NLtree_CONUS, scale = FALSE) %>%
  rename(percNLTree_CONUS = "modelPreds")
# NLtree_GS_predsOverTime<- climDat_time_Pred %>%
#   mutate(percNLTree_GS = 0)
  # makePredictions(predictionDF = climDat_time_Pred,
  #                                           modelObject = NLtree_GS) %>%
  # rename(percNLTree_GS = "modelPreds")

C3grass_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = C3grass_CONUS, scale = FALSE) %>%
  rename(percC3grass_CONUS = "modelPreds")

C4grass_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = C4grass_CONUS, scale = FALSE) %>%
  rename(percC4grass_CONUS = "modelPreds")

forb_CONUS_predsOverTime<- makePredictions(predictionDF = climDat_time_Pred,
                                            modelObject = forb_CONUS, scale = FALSE) %>%
  rename(percForb_CONUS = "modelPreds")

predsOverTime <- totalHerb_CONUS_predsOverTime  %>%
  #cbind(totalHerb_GS_predsOverTime %>% select(absTotalHerb_GS)) %>%
  cbind(totalTree_F_predsOverTime %>% select(absTotalTree_F)) %>%
  cbind(totalTree_GS_predsOverTime %>% select(absTotalTree_GS)) %>%
  cbind(bareGround_CONUS_predsOverTime %>% select(absBareGround_CONUS)) %>%
  cbind(shrub_CONUS_predsOverTime %>% select(absShrub_CONUS)) %>%
  cbind(BLtree_CONUS_predsOverTime %>% select(percBLTree_CONUS)) %>%
  #cbind(BLtree_GS_predsOverTime %>% select(percBLTree_GS)) %>%
  cbind(NLtree_CONUS_predsOverTime %>% select(percNLTree_CONUS)) %>%
  #cbind(NLtree_GS_predsOverTime %>% select(percNLTree_GS)) %>%
  cbind(C3grass_CONUS_predsOverTime %>% select(percC3grass_CONUS)) %>%
  cbind(C4grass_CONUS_predsOverTime %>% select(percC4grass_CONUS)) %>%
  cbind(forb_CONUS_predsOverTime %>% select(percForb_CONUS)) %>%
  cbind(preds_byHand_time %>% select(pred_binary, pred_contNew, zeroTree_binary)) %>%
  rename(prob_Forest_b = "pred_binary",
         prob_Forest_c = "pred_contNew") %>%
  mutate(prob_grassShrub_b = 1 - prob_Forest_b,
         prob_grassShrub_c = 1 - prob_Forest_c)



# for total herbaceous and total tree, scale according to ecoregion
predsOverTime <- makePredsCONUSwide(predsOverTime)

```

```{r fig.width=9, fig.height = 10.5}
# change spatialIDs to discrete numbers, since the long form is causing some sort of floating point issue?
spatID_lu <- data.frame("spatialID" = unique(predsOverTime$spatialID),
                        "uniqueID" = 1:length(predsOverTime$spatialID))
predsOverTime <- predsOverTime %>%
  left_join(spatID_lu)

## for locations that are unforested (and therefore have values of 0 always for each tree type), change those values to 0.00001 so the values aren't NAN
predsOverTime <- predsOverTime %>%
  mutate(absTotalTree_CONUS = replace(absTotalTree_CONUS, absTotalTree_CONUS == 0, 0.00001),
         percBLTree_scaled_CONUS = replace(percBLTree_scaled_CONUS, percBLTree_scaled_CONUS == 0, 0.00001),
         percNLTree_scaled_CONUS = replace(percNLTree_scaled_CONUS, percNLTree_scaled_CONUS == 0, 0.00001),
         absNLTree_CONUS = replace(absNLTree_CONUS, absNLTree_CONUS == 0, 0.00001),
         absBLTree_CONUS = replace(absBLTree_CONUS, absBLTree_CONUS == 0, 0.00001),
         absBLTree_CONUS_scaled = replace(absBLTree_CONUS_scaled, absBLTree_CONUS_scaled == 0, 0.00001),
         absNLTree_CONUS_scaled = replace(absNLTree_CONUS_scaled, absNLTree_CONUS_scaled == 0, 0.00001),
         )
# make into a long format
predsOverTime_long <- predsOverTime %>%
  select(year,x, y, spatialID, uniqueID, absShrub_CONUS, absBareGround_CONUS, absTotalHerb_CONUS, absTotalTree_CONUS, absNLTree_CONUS:absForb_CONUS) %>%
  pivot_longer(cols = c(absShrub_CONUS, absBareGround_CONUS, absTotalHerb_CONUS, absTotalTree_CONUS, absNLTree_CONUS:absForb_CONUS)) %>%
  mutate(year = as.integer(year))

# calculate the coefficient of variation for each cover type at each location
predsOverTime_cv <- predsOverTime_long %>%
  group_by(uniqueID, name) %>%
  summarize(cv = sd(value)/mean(value))
## add x,y
predsOverTime_cv <- predsOverTime_cv %>%
  left_join(predsOverTime %>% select(uniqueID, x, y))

# visualize change over time for a selection of points
coverOverTimeForSelectLocations <- predsOverTime_long %>%
  filter(uniqueID %in% 1:50) %>%
  filter(name %in% c("absShrub_CONUS", "absBareGround_CONUS",  "absNLTree_CONUS",
                     "absBLTree_CONUS", "absC3grass_CONUS", "absC4grass_CONUS", "absForb_CONUS")) %>%
  ggplot() +
  facet_wrap(~uniqueID) +
  geom_area(aes(x = year, y = value, fill = name)) +
  geom_line(data = predsOverTime_long %>%
              filter(uniqueID %in% 1:50) %>%
              filter(name %in% c("absTotalHerb_CONUS", "absTotalTree_CONUS")),
            aes(x = year, y = value, linetype = name), col = "black") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 90)) +
  labs(title = "Change in modeled absolute cover by functional type from \n 2010 to 2020 for a subset of locations")
mapOfSelectLocations <- predsOverTime_long %>%
    select(x, y, uniqueID) %>%
    filter(uniqueID %in% 1:50) %>%
    unique() %>%
    ggplot() +
    geom_sf(data = cropped_states, aes()) +
    geom_point(aes(x, y)) +
    geom_label_repel(aes(x, y, label = uniqueID)) +
    labs(title = "Locations in above plot")


ggarrange(coverOverTimeForSelectLocations, mapOfSelectLocations, ncol = 1, heights = c(1,.5))
```
```{r fig.width=9, fig.height = 8}
predsOverTime_cv %>%
  ggplot() +
  geom_boxplot(aes(x = name, y = cv, col = name)) +
  theme_minimal() +
  labs(title = "Coefficients of variation of model-predicted absolute \n cover at 1000 random locations for each functional group from 2010-2020",
        x = "functional group",
       y = "coefficient of variation") +
  theme(axis.text.x = element_text(angle = 90))

# show the locations of each point that is being used for this temporal analysis
#cropped_states <- cropped_states %>%
 # rename(geometry = Shape)

cropped_states2 <- cropped_states %>%
  rename(name2 = name)
 library(hexbin)
ggplot() +
  facet_wrap(~name) +
  geom_sf(data = cropped_states2, aes()) +
  stat_summary2d(data = predsOverTime_cv, aes(x = x, y = y, z = cv), fun = ~ mean(.x, na.rm = TRUE)) +
  geom_hex(data = predsOverTime_cv, aes(x = x, y = y, col = cv), stat = "mean", na.rm = TRUE) +
  guides(fill = guide_legend(title = "CV")) +
  theme_minimal() +
  labs(title = "The spatial distribution of the coefficients of variation of \nmodel-predicted cover from 2010-2020 for 1000 random locations",
       subtitle = "Note that values are aggregated across space to enhance readability")
```

# Now, we generate CONUS-wide model predictions of *relative* cover

We use the model predictions of absolute cover we generated above, which are CONUS-wide and have been scaled according to predicted ecoregions, to calculate relative cover for each functional group. We consider the overstory (trees) and understory (all other functional types) separately in our relativization scheme, such that the values of relative cover are identical to the values of absolute cover for total trees, needle-leaved trees, and broad-leaved trees. For the understory (comprised of all herbaceous functional types, shrubs, and bare ground), total relative cover must sum to 1, so absolute cover for these functional groups is scaled accordingly.

```{r}
## for contemporary data
contempPreds <- contempPreds %>%
  # creating the 'scaling factor' we use to make all other absolute cover relative
  mutate(total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + #absTotalTree_CONUS +
                                           absBareGround_CONUS + absShrub_CONUS)) %>%
  mutate(relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS
         )

## for first climate model
predsFuture1 <- predsFuture1 %>%
  mutate(total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + #absTotalTree_CONUS +
                                           absBareGround_CONUS + absShrub_CONUS)) %>%
  mutate(relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS
         )

## for second climate model
predsFuture2 <- predsFuture2 %>%
  mutate(total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + #absTotalTree_CONUS +
                                           absBareGround_CONUS + absShrub_CONUS)) %>%
  mutate(relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS
         )
```

Below are maps of predicted relative cover for each functional group we modeled.
These maps show model-predicted relative cover using contemporary climate and weather data, residuals when comparing these contemporary predictions to observations, and changes in model predictions of cover when predictions are made using simulated future climate and weather data from two GCMs.
This figure also includes a map of the total non-tree absolute cover, which was the scaling factor used to calculate relative cover for each non-tree group. Note that relative cover maps for total trees, needle-leaved trees, and broad-leaved trees are not shown, since, for those functional groups, relative cover is identical to absolute cover.

Note that maps of residuals for all of these cover types are restricted only to locations where we have both total herbaceous, shrub, and bare ground cover information. This is because relative cover is calculated by dividing cover of the functional group of interest by the total non-tree cover (herbaceous + shrub + bare ground cover), so these values can only be calculated for locations where we have data for all three components of non-tree cover.
```{r making maps of relative cover}
# list of absolute cover names
responseNames <- c(#"relCoverB_totalTree",
  "relCoverB_totalHerb", "relCoverB_shrub", "relCoverB_bareGround",
                   #"relCoverB_NLTree", "relCoverB_BLTree",
  "relCoverB_C3Grass", "relCoverB_C4Grass", "relCoverB_Forb",
                   "total_NonTree_AbsCover_CONUS")

# prepare the observed data for raster calculations (calculate relative observed cover)
obsDat <- obsDat %>%
  mutate(
       total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + absShrub_CONUS + absBareGround_CONUS) # for observations
       ) %>%
  mutate(
  # making observations relative
         relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS,
         )


relCover_B_Maps_contemp <-  lapply(responseNames,
                                      FUN = function(x) {
                                       ## contemporary absolute cover
                                        # rasterize response
                                        resp_rast <- contempPreds %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_2 <- resp_rast %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        # make a map
                                        resp_map <- ggplot() +
                                          geom_spatraster(data = resp_rast_2, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Predictions of absolute cover of \n",x),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"),
                                                         x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                           scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(
                   axis.text = element_blank(),
                   strip.text = element_text(size = 15),
                   title = element_text(size = 18),
                   axis.ticks = element_blank())

  # add an inset showing a density plot
           # get data
           data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
             contempPreds %>%
                st_drop_geometry() %>%
             select(x) %>%
            pivot_longer(c(x), names_to = "lyr", values_to = "Cover")

           # make insets
           inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_map <- resp_map + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

                                        ## calculate residuals
                                        # make maps of residuals between absolute cover predictions and observations
                                        # rasterize observations
                                        plotObservations <- obsDat %>%
                                          dplyr::filter(is.finite(.data[[x]])) %>% 
                                          dplyr::filter(!is.nan(.data[[x]])) %>% 
                                          terra::vect(geom = c("x", "y")) %>%
                                          terra::set.crs(crs(crs_veg)) %>%
                                          terra::project(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                         plotObservations <- plotObservations %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        # calculate residuals (observed - predicted)
                                        resids_rast <- plotObservations - resp_rast_2
                                        ## aggregate the residuals raster to make it easier to see
                                        resids_rastCoarse <- resids_rast %>%
                                          aggregate(fact = 8, fun = "mean", na.rm = TRUE)
                                        # map the residuals
                                        mapResids <- ggplot() +
                                          geom_spatraster(data = resids_rastCoarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Residuals (observations - predicted absolute cover) \nfor ",x),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("red", "orange", "white", "green", "blue"),
                                                                #values = c(-1, -.5, 0, 0.5, 1), 
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          
                                           # scale_fill_gradient2(low = "red",
                                           #                     mid = "white" ,
                                           #                     high = "blue" ,
                                           #                     midpoint = 0,   na.value = "lightgrey",
                                           #                     limits = c(-1,1) )+
                                                                 theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )
                                        # add an inset showing a density plot
           # get data
           data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
            data.frame("resids" = terra::values(resids_rast$mean, na.rm = TRUE, mat = FALSE)) %>%
             select(resids) %>%
            pivot_longer(c(resids), names_to = "lyr", values_to = "Cover")

           # make insets
           inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         mapResids <- mapResids + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

                                        
                                        # make maps of % CHANGE residuals between absolute cover predictions and observations
                                        # rasterize observations
                                        # calculate residuals (observed - predicted)/obs
                                        resids_rast_CHANGE <- (plotObservations - resp_rast_2)/plotObservations
                                        ## aggregate the residuals raster to make it easier to see
                                        resids_rast_CHANGE_Coarse <- resids_rast_CHANGE %>%
                                          aggregate(fact = 8, fun = "mean", na.rm = TRUE)
                                        # map the residuals
                                        mapResids_CHANGE <- ggplot() +
                                          geom_spatraster(data = resids_rast_CHANGE_Coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Relative Residuals \n(observations - predicted absolute cover)/observations \nfor ",x),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                           scale_fill_gradient2(low = "red",
                                                               mid = "white" ,
                                                               high = "blue" ,
                                                               midpoint = 0,   na.value = "lightgrey")+
                                                                 theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )
                                        # add an inset showing a density plot
           # get data
           data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
            data.frame("resids" = terra::values(resids_rast_CHANGE$mean, na.rm = TRUE, mat = FALSE)) %>%
             select(resids) %>%
            pivot_longer(c(resids), names_to = "lyr", values_to = "Cover")

           # make insets
           inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         mapResids_CHANGE <- mapResids_CHANGE + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

                                         ## future model 1 absolute cover
                                        # rasterize response
                                        resp_rast_future1 <- predsFuture1 %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_future1_2 <- resp_rast_future1 %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        resp_rast_future1_coarse <- resp_rast_future1_2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        # calculate ABSOLUTE differences between contemporary preds and future 1 preds (deltas)
                                        rast_deltas_future1 <- resp_rast_future1_2 - resp_rast_2
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future1_coarse <- rast_deltas_future1 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        
                                        ## calculate RELATIVE differences between contemporary preds and future 1 preds (deltas) (future - current)/current
                                        rast_deltas_future1_REL <- (resp_rast_future1_2 - resp_rast_2)/(resp_rast_2+.000001)
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future1_REL_coarse <- rast_deltas_future1_REL %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)

                                        # make a map of the actual predicted values for modeled climate 1
                                        resp_Actual_map_future1 <- ggplot() +
                                          geom_spatraster(data = resp_rast_future1_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n - based on data from climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
                                          predsFuture1 %>%
                                          dplyr::select(all_of(x)) %>%
                                          pivot_longer(c(x), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_Actual_map_future1 <- resp_Actual_map_future1 + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 
                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future1 <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future1_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("#f89540", "#cc4778", "white","#7e03a8", "#0d0887"),
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          # scale_fill_gradient2(low = "orange",
                                          #                      mid = "white" ,
                                          #                      high = "purple" ,
                                          #                      midpoint = 0, limits = c(-1,1),  na.value = "lightgrey")  +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )
                                      


                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future1$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
           inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_delta_map_future1 <- resp_delta_map_future1 + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

                                        ## RELATIVE deltas for model 1
                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future1_REL <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future1_REL_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          # scale_fill_gradientn(colors = c("#f89540",  "white", "#0d0887"),
                                          #                       #breaks = c(-1, -.5, 0, 0.5, 1), 
                                          #                      #limits = c(-1,1),
                                          #                       na.value = "lightgrey") + 
                                          scale_fill_gradient2(low = "#f89540",
                                                               mid = "white" ,
                                                               high = "#0d0887" ,
                                                               midpoint = 0, #limits = c(-1,1),  
                                                               na.value = "lightgrey")  +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future1_REL$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
           inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_delta_map_future1_REL <- resp_delta_map_future1_REL + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 
         
                                        ## future model 2 absolute cover
                                        # rasterize response
                                        resp_rast_future2 <- predsFuture2 %>%
                                          terra::vect() %>%
                                          terra::set.crs(crs(test_rastTemp)) %>%
                                          terra::rasterize(y = test_rastTemp,
                                                           field = x,
                                                           fun = mean, na.rm = TRUE)
                                        # get the extent of this particular raster, and crop it accordingly
                                        resp_rast_future2_2 <- resp_rast_future2 %>%
                                          crop(ext(min(tempExt[,1]), max(tempExt[,1]),
                                                   min(tempExt[,2]), max(tempExt[,2]))
                                               )
                                        resp_rast_future2_coarse <- resp_rast_future2_2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        # calculate differences between contemporary preds and future 1 preds (deltas)
                                        rast_deltas_future2 <- resp_rast_future2_2 - resp_rast_2
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future2_coarse <- rast_deltas_future2 %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)

                                          ## calculate RELATIVE differences between contemporary preds and future 1 preds (deltas) (future - current)/current
                                        rast_deltas_future2_REL <- (resp_rast_future2_2 - resp_rast_2)/(resp_rast_2+.000001)
                                        # temporarily aggregate to coarser raster for visualization purposes
                                        rast_deltas_future2_REL_coarse <- rast_deltas_future2_REL %>%
                                          aggregate(fact = 12, fun = "mean", na.rm = TRUE)
                                        
                                        # make a map of the actual predicted values for modeled climate 1
                                        resp_Actual_map_future2 <- ggplot() +
                                          geom_spatraster(data = resp_rast_future2_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n - based on data from climate model: IPSL-CM5A-MR (France)"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(
                                             colors = c("wheat", "tan", "forestgreen"),
                                             values = c(0, 1e-10, 1),
                                             limits = c(0,1),
                                             na.value = "lightgrey"
                                             ) +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- #terra::values(dat_1_RAST$RAP[[y]][[1:4]]) %>%
                                          predsFuture2 %>%
                                          dplyr::select(all_of(x)) %>%
                                          pivot_longer(c(x), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_Actual_map_future2 <- resp_Actual_map_future2 + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 
                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future2 <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future2_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: IPSL-CM5A-MR (France)"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          scale_fill_gradientn(colors = c("#f89540", "#cc4778", "white","#7e03a8", "#0d0887"),
                                                                breaks = c(-1, -.5, 0, 0.5, 1), 
                                                               limits = c(-1,1),
                                                                na.value = "lightgrey") + 
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future2$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                       inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_delta_map_future2 <- resp_delta_map_future2 + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

 ## RELATIVE deltas for model 1
                                        # make a map of the deltas of predicted values for modeled climate 1
                                        resp_delta_map_future2_REL <- ggplot() +
                                          geom_spatraster(data = rast_deltas_future2_REL_coarse, maxcell = 50000000 ) +
                                          geom_sf(data=cropped_states %>% st_transform(crs = st_crs(test_rastTemp)) %>%
                                                    st_crop(st_bbox(resp_rast_2)) ,fill=NA ) +
                                                    labs(title = paste0("Future predictions of absolute cover of ",x,", \n compared to contemporary predictions (future - contemprary)  \n - climate model: BNU-ESM"),
                                                         subtitle = c("Values shown are the predictions \nwithin each 1 km x 1 km grid cell"), x = "", y = "" ) +
                                          xlim(-2150000, 2510000) +
                                          ylim(-2005000, 1000000) +
                                          coord_sf(datum = st_crs(test_rastTemp)) +
                                          # scale_fill_gradientn(colors = c("#f89540",  "white", "#0d0887"),
                                          #                       #breaks = c(-1, -.5, 0, 0.5, 1), 
                                          #                      #limits = c(-1,1),
                                          #                       na.value = "lightgrey") + 
                                          scale_fill_gradient2(low = "#f89540",
                                                               mid = "white" ,
                                                               high = "#0d0887" ,
                                                               midpoint = 0, #limits = c(-1,1),  
                                                               na.value = "lightgrey")  +
                                          theme(axis.text = element_blank(),
                                                                       strip.text = element_text(size = 15),
                                                                       title = element_text(size = 18),
                                                                       axis.ticks = element_blank()
                                                                       )

                                        # add an inset showing a density plot
                                        # get data
                                        data_inset <- data.frame("deltas" = terra::values(rast_deltas_future2_REL$mean, na.rm = TRUE, mat = FALSE)) %>%
                                          pivot_longer(c(deltas), names_to = "lyr", values_to = "Cover")
                                        # make insets
                                        inset_plots <- ggplot(data_inset) +
   geom_density(aes(Cover)) +
      theme_bw(base_size=9) +  ## makes everything smaller
      theme(panel.background = element_rect(fill="transparent"),  ## white plot background
            panel.border = element_blank(),
axis.title.y = element_blank(),
axis.title.x = element_blank(),
axis.text.x = element_text(size=rel(2)), ## tiny axis text
axis.text.y = element_text(size=rel(2)), ## tiny axis text
axis.line.x = element_line(size = .5),
axis.line.y = element_line(size = .5),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
plot.background = element_blank(),
strip.background = element_blank(),
strip.text = element_blank())
                
         resp_delta_map_future2_REL <- resp_delta_map_future2_REL + annotation_custom(grob = ggplotGrob(inset_plots), -2025000, ymax=-1205000, xmin=-2280000, xmax=-180000) 

                                        return(list("rast" = resp_rast_2,
                                                    "map" = resp_map,
                                                    
                                                    "rast_resids" = resids_rast,
                                                    "map_resids" = mapResids,
                                                    
                                                    "rast_resids_relative" = resids_rast_CHANGE,
                                                    "map_resids_relative" = mapResids_CHANGE,
                                                    
                                                    "rast_future1" = resp_rast_future1_2,
                                                    "map_future1" = resp_Actual_map_future1,
                                                    "map_future1_deltas" = resp_delta_map_future1,
                                                    "map_future1_deltas_relative" = resp_delta_map_future1_REL,
                                                    
                                                    "rast_future2" = resp_rast_future2_2,
                                                    "map_future2" = resp_Actual_map_future2,
                                                    "map_future2_deltas" = resp_delta_map_future2,
                                                    "map_future2_deltas_relative" = resp_delta_map_future2_REL))
                                      })


names(relCover_B_Maps_contemp) <- c(#"relCoverB_totalTree",
  "relCoverB_totalHerb", "relCoverB_shrub", "relCoverB_bareGround",
                   #"relCoverB_NLTree", "relCoverB_BLTree",
  "relCoverB_C3Grass", "relCoverB_C4Grass", "relCoverB_Forb",
                   "total_NonTree_AbsCover_CONUS")

rm(contempPreds, predsFuture1, predsFuture2)
gc()
```

```{r rendering maps of relative cover, fig.height=20, fig.width=18}
(relCover_B_Maps_contemp$relCoverB_totalHerb$map + relCover_B_Maps_contemp$relCoverB_totalHerb$map_resids)/
 (relCover_B_Maps_contemp$relCoverB_totalHerb$map_future1 + relCover_B_Maps_contemp$relCoverB_totalHerb$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCoverB_totalHerb$map_future2+ relCover_B_Maps_contemp$relCoverB_totalHerb$map_future2_deltas)
```

---
```{r  fig.height=20, fig.width=18}
(relCover_B_Maps_contemp$relCoverB_shrub$map + relCover_B_Maps_contemp$relCoverB_shrub$map_resids)/
 (relCover_B_Maps_contemp$relCoverB_shrub$map_future1 + relCover_B_Maps_contemp$relCoverB_shrub$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCoverB_shrub$map_future2+ relCover_B_Maps_contemp$relCoverB_shrub$map_future2_deltas)
```
---
```{r  fig.height=20, fig.width=18}

(relCover_B_Maps_contemp$relCoverB_bareGround$map + relCover_B_Maps_contemp$relCoverB_bareGround$map_resids)/
 (relCover_B_Maps_contemp$relCoverB_bareGround$map_future1 + relCover_B_Maps_contemp$relCoverB_bareGround$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCoverB_bareGround$map_future2+ relCover_B_Maps_contemp$relCoverB_bareGround$map_future2_deltas)
```
---
```{r  fig.height=20, fig.width=18}

(relCover_B_Maps_contemp$relCoverB_C3Grass$map + relCover_B_Maps_contemp$relCoverB_C3Grass$map_resids)/
 (relCover_B_Maps_contemp$relCoverB_C3Grass$map_future1 + relCover_B_Maps_contemp$relCoverB_C3Grass$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCoverB_C3Grass$map_future2+ relCover_B_Maps_contemp$relCoverB_C3Grass$map_future2_deltas)
```
---
```{r  fig.height=20, fig.width=18}

(relCover_B_Maps_contemp$relCoverB_C4Grass$map + relCover_B_Maps_contemp$relCoverB_C4Grass$map_resids)/
 (relCover_B_Maps_contemp$relCoverB_C4Grass$map_future1 + relCover_B_Maps_contemp$relCoverB_C4Grass$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCoverB_C4Grass$map_future2+ relCover_B_Maps_contemp$relCoverB_C4Grass$map_future2_deltas)
```
---
```{r  fig.height=20, fig.width=18}

(relCover_B_Maps_contemp$relCover_B_Forb$map + relCover_B_Maps_contemp$relCover_B_Forb$map_resids)/
 (relCover_B_Maps_contemp$relCover_B_Forb$map_future1 + relCover_B_Maps_contemp$relCover_B_Forb$map_future1_deltas)/
  (relCover_B_Maps_contemp$relCover_B_Forb$map_future2+ relCover_B_Maps_contemp$relCover_B_Forb$map_future2_deltas)
```
---
```{r  fig.height=20, fig.width=18}

(relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map + relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map_resids)/
 (relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map_future1 + relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map_future1_deltas)/
  (relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map_future2+ relCover_B_Maps_contemp$total_NonTree_AbsCover_CONUS$map_future2_deltas)
```

```{r, eval = TRUE}
## save the rasterized absolute cover values for contemporary data
for(i in 1:length(responseNames)) {
  if ( i == 1) {
    outRast <- relCover_B_Maps_contemp[[responseNames[i]]]$rast
  } else {
    outRast <- c(outRast, relCover_B_Maps_contemp[[responseNames[i]]]$rast)
  }
}
names(outRast) <- responseNames

## save for later use
terra::writeRaster(outRast, filename = "../../../Data_processed/CoverData/finalCoverData/finalRelativeCoverData_2023dayMetClimateData.tif", overwrite = TRUE)

rm(relCover_B_Maps_contemp)
gc()
```

Now, we show quantile plots for model predictions of relative cover made with contemporary data. These plots compare final predictions of relative cover to observed relative cover. Quantile plots for relative cover of tree functional types are now show, since they are identical to those for absolute cover.
```{r preparing data for quantile plots for relative cover}
# First, we need to relativize the predictions and the observations (stored in preds_quantiles)
preds_quantile <- preds_quantile %>%
mutate(total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + #absTotalTree_CONUS +
                                         absBareGround_CONUS + absShrub_CONUS), # for model predictions
       total_NonTree_AbsCover_obs = (TotalHerbaceousCover + ShrubCover + BareGroundCover) # for observations
       ) %>%
  # making model predictions relative
  mutate(relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS,
  # making observations relative
         relCoverB_totalTree_Obs = TotalTreeCover,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb_Obs = TotalHerbaceousCover/total_NonTree_AbsCover_obs,
         relCoverB_shrub_Obs = ShrubCover/total_NonTree_AbsCover_obs,
         relCoverB_bareGround_Obs = BareGroundCover/total_NonTree_AbsCover_obs,
         relCoverB_NLTree_Obs = NLTreeCover_abs,
         relCoverB_BLTree_Obs = BLTreeCover_abs,
         relCoverB_C3Grass_Obs = C3GramCover_abs/total_NonTree_AbsCover_obs,
         relCoverB_C4Grass_Obs = C4GramCover_abs/total_NonTree_AbsCover_obs,
         relCoverB_Forb_Obs = ForbCover_abs/total_NonTree_AbsCover_obs,
         )
```

```{r calculating quantiles for relative cover}
## exclude trees, since the predictions are the same as the absolute cover shown above
# shrubs
# unique(c(names(coefficients(shrub_CONUS))))
deciles_shrub_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_shrub_Obs", "relCoverB_shrub",decilePredNames_shrub ) %>% drop_na(),
                                  response_vars = c("relCoverB_shrub_Obs", "relCoverB_shrub"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_shrub), # for predictors, combine list of predictors for all models
cut_points = seq(0, 1, 0.01)
                                     )

# bare ground
# unique(c(names(coefficients())))
deciles_bareGround_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_bareGround_Obs", "relCoverB_bareGround", decilePredNames_bareGround) %>% drop_na(),
                                       response_vars = c("relCoverB_bareGround_Obs", "relCoverB_bareGround"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_bareGround), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )
# total herbaceous cover
#unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS))))
deciles_totalHerb_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_totalHerb_Obs", "relCoverB_totalHerb",decilePredNames_totalHerb) %>% drop_na(),
                response_vars = c("relCoverB_totalHerb_Obs", "relCoverB_totalHerb"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_totalHerb), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# forbs
# unique(c(names(coefficients(totalTree_F)), names(coefficients(totalTree_GS)), names(coefficients(BLtree_GS)), names(coefficients(BLtree_F))))
deciles_forb_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_Forb_Obs", "relCoverB_Forb", decilePredNames_forb) %>% drop_na(),
                                 response_vars = c("relCoverB_Forb_Obs", "relCoverB_Forb"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_forb), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

# C3 grass
# unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS)), names(coefficients(C3grass_CONUS))))
deciles_C3grass_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_C3Grass_Obs", "relCoverB_C3Grass", decilePredNames_C3Grass) %>% drop_na(),
                                    response_vars = c("relCoverB_C3Grass_Obs", "relCoverB_C3Grass"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_C3Grass), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )
# C4 grass
# unique(c(names(coefficients(totalHerb_F)), names(coefficients(totalHerb_GS)), names(coefficients(C3grass_CONUS))))
deciles_C4grass_Obs <- predvars2deciles(df = preds_quantile %>% select("relCoverB_C4Grass_Obs", "relCoverB_C4Grass", decilePredNames_C4Grass) %>% drop_na(),
                                    response_vars = c("relCoverB_C4Grass_Obs", "relCoverB_C4Grass"), # name of observations, followed by name of predictions
  pred_vars = c(decilePredNames_C4Grass), # for predictors, combine list of predictors for all models (total tree for GS and F, and perc BL tree for GS and F)
cut_points = seq(0, 1, 0.01)
                                     )

```

```{r making quantile plots for relative cover}

# shrubs
quantPlot_shrubs_Obs <- decile_dotplot_pq(df = deciles_shrub_Obs, response= c("relCoverB_shrub_Obs", "relCoverB_shrub"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative Shrub Cover: Quantile Plot")

quantPlot_shrubs_Obs <- add_dotplot_inset(quantPlot_shrubs_Obs, df = deciles_shrub_Obs, response = c("relCoverB_shrub_Obs", "relCoverB_shrub"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

# bare ground
quantPlot_bareGround_Obs <- decile_dotplot_pq(df = deciles_bareGround_Obs, response= c("relCoverB_bareGround_Obs", "relCoverB_bareGround"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative Bare Ground Cover: Quantile Plot")

quantPlot_bareGround_Obs <- add_dotplot_inset(quantPlot_bareGround_Obs, df = deciles_bareGround_Obs, response = c("relCoverB_bareGround_Obs", "relCoverB_bareGround"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

# total herbaceous
quantPlot_totHerb_Obs <- decile_dotplot_pq(df = deciles_totalHerb_Obs, response= c("relCoverB_totalHerb_Obs", "relCoverB_totalHerb"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative Total Herbaceous Cover: Quantile Plot")

quantPlot_totHerb_Obs <- add_dotplot_inset(quantPlot_totHerb_Obs, df = deciles_totalHerb_Obs, response = c("relCoverB_totalHerb_Obs", "relCoverB_totalHerb"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

# forbs
quantPlot_forbs_Obs <- decile_dotplot_pq(df = deciles_forb_Obs, response= c("relCoverB_Forb_Obs", "relCoverB_Forb"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative Forb Cover: Quantile Plot")

quantPlot_forbs_Obs <- add_dotplot_inset(quantPlot_forbs_Obs, df = deciles_forb_Obs, response = c("relCoverB_Forb_Obs", "relCoverB_Forb"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

# C3 grass
quantPlot_c3grass_Obs <- decile_dotplot_pq(df = deciles_C3grass_Obs, response= c("relCoverB_C3Grass_Obs", "relCoverB_C3Grass"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative C3 Grass Cover: Quantile Plot")

quantPlot_c3grass_Obs <- add_dotplot_inset(quantPlot_c3grass_Obs, df = deciles_C3grass_Obs, response = c("relCoverB_C3Grass_Obs", "relCoverB_C3Grass"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)

#C4 grass
quantPlot_c4grass_Obs <- decile_dotplot_pq(df = deciles_C4grass_Obs, response= c("relCoverB_C4Grass_Obs", "relCoverB_C4Grass"), IQR = FALSE,
                        CI = TRUE
                        ) + ggtitle("Relative C4 Grass Cover: Quantile Plot")

quantPlot_c4grass_Obs <- add_dotplot_inset(quantPlot_c4grass_Obs, df = deciles_C4grass_Obs, response = c("relCoverB_C4Grass_Obs", "relCoverB_C4Grass"), dfRaw = preds_quantile, add_smooth = TRUE, deciles = FALSE)
```

```{r rendering quantile plots for relative cover, fig.height = 10, fig.width = 10, warning=FALSE, message=FALSE}
## now, display the figures
quantPlot_totHerb_Obs
quantPlot_shrubs_Obs
quantPlot_bareGround_Obs
quantPlot_c3grass_Obs
quantPlot_c4grass_Obs
quantPlot_forbs_Obs
```

Below, we show the RMSE and bias of the final estimates of relative cover for each functional type (except trees) in comparison to the observed data.
```{r}
long_absPreds <- preds_quantile %>%
  mutate(uniqueID = 1:nrow(preds_quantile)) %>%
  select(uniqueID, relCoverB_totalHerb, relCoverB_shrub,
relCoverB_bareGround, relCoverB_C3Grass,
relCoverB_C4Grass, relCoverB_Forb) %>%
  rename(TotalHerb = relCoverB_totalHerb,
         Shrub = relCoverB_shrub,
         BareGround = relCoverB_bareGround,
         C3gram = relCoverB_C3Grass,
         C4gram = relCoverB_C4Grass,
         Forb = relCoverB_Forb) %>%
  pivot_longer(cols = c(TotalHerb, Shrub, BareGround, C3gram, C4gram, Forb),
                names_to = "names",
               values_to = "preds_values"
               ) %>%
  select(uniqueID, names, preds_values)

long_absObs <- preds_quantile %>%
  mutate(uniqueID = 1:nrow(preds_quantile)) %>%
  select(uniqueID, relCoverB_totalHerb_Obs, relCoverB_shrub_Obs,  relCoverB_bareGround_Obs, relCoverB_C3Grass_Obs, relCoverB_C4Grass_Obs,  relCoverB_Forb_Obs) %>%
  rename(TotalHerb = relCoverB_totalHerb_Obs,
         Shrub = relCoverB_shrub_Obs,
         BareGround = relCoverB_bareGround_Obs,
         C3gram = relCoverB_C3Grass_Obs,
         C4gram = relCoverB_C4Grass_Obs,
         Forb = relCoverB_Forb_Obs) %>%
  pivot_longer(cols = c(TotalHerb, Shrub, BareGround, C3gram, C4gram, Forb),
                names_to = "names",
               values_to = "obs_values"
               ) %>%
  select(uniqueID, names, obs_values)

longObsPreds <- long_absObs %>%
  left_join(long_absPreds)

## calculate bias
(summaryTableAbs <- longObsPreds %>%
  mutate(resids = obs_values - preds_values) %>%
  group_by(names) %>%
  dplyr::summarise(bias = mean(resids, na.rm = TRUE),
            RMSE = round(sqrt(mean(resids^2, na.rm = TRUE)),4)
            ) %>%
  slice(8, 9, 7, 2, 6, 1, 3, 4, 5) %>% kable(
  format = "html",
  col.names = c("Model Name", "Model bias mean(obs. - pred.)", "model RMSE")
))
```

Finally, we show how these model predictions made using contemporary climate and weather data change over time from 2010 to 2020.
```{r change in relative cover predictions over time, fig.width=9, fig.height = 10.5}
# calculate relativized cover (version w/ all functional groups summing to 1)
predsOverTime <- predsOverTime %>%
mutate(total_NonTree_AbsCover_CONUS = (absTotalHerb_CONUS + #absTotalTree_CONUS +
                                         absBareGround_CONUS + absShrub_CONUS)) %>%
  mutate(relCoverB_totalTree = absTotalTree_CONUS,#/total_NonTree_AbsCover_CONUS,
         relCoverB_totalHerb = absTotalHerb_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_shrub = absShrub_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_bareGround = absBareGround_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_NLTree = absNLTree_CONUS,
         relCoverB_BLTree = absBLTree_CONUS,
         relCoverB_C3Grass = absC3grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_C4Grass = absC4grass_CONUS/total_NonTree_AbsCover_CONUS,
         relCoverB_Forb = absForb_CONUS/total_NonTree_AbsCover_CONUS
         )

# make into a long format
predsOverTime_long <- predsOverTime %>%
  select(year,x, y, spatialID, uniqueID, total_NonTree_AbsCover_CONUS:relCoverB_Forb) %>%
  pivot_longer(cols = c(total_NonTree_AbsCover_CONUS:relCoverB_Forb)) %>%
  mutate(year = as.integer(year))

# calculate the coefficient of variation for each cover type at each location
predsOverTime_cv <- predsOverTime_long %>%
  group_by(uniqueID, name) %>%
  summarize(cv = sd(value)/mean(value))
## add x,y
predsOverTime_cv <- predsOverTime_cv %>%
  left_join(predsOverTime %>% select(uniqueID, x, y))

# visualize change over time for a selection of points
coverOverTimeForSelectLocations <- predsOverTime_long %>%
  filter(uniqueID %in% 1:50) %>%
  filter(name %in% c( #"total_NonTree_AbsCover_CONUS",  "relCoverB_totalTree",
#"relCoverB_totalHerb",
    "relCoverB_shrub"    , "relCoverB_bareGround" , "relCoverB_NLTree",  "relCoverB_BLTree",
"relCoverB_C3Grass" , "relCoverB_C4Grass",   "relCoverB_Forb" )) %>%
  ggplot() +
  facet_wrap(~uniqueID) +
  geom_area(aes(x = year, y = value, fill = name)) +
  geom_line(data = predsOverTime_long %>%
              filter(uniqueID %in% 1:50) %>%
              filter(name %in% c("relCoverB_totalHerb", "relCoverB_totalTree")),
            aes(x = year, y = value, linetype = name), col = "black") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 90)) +
  labs(title = "Change in modeled relative cover (all Non-tree types sum to 1) by functional type from \n 2010 to 2020 for a subset of locations")
mapOfSelectLocations <- predsOverTime_long %>%
    select(x, y, uniqueID) %>%
    filter(uniqueID %in% 1:50) %>%
    unique() %>%
    ggplot() +
    geom_sf(data = cropped_states, aes()) +
    geom_point(aes(x, y)) +
    geom_label_repel(aes(x, y, label = uniqueID)) +
    labs(title = "Locations in above plot")


ggarrange(coverOverTimeForSelectLocations, mapOfSelectLocations, ncol = 1, heights = c(1,.5))
```
```{r fig.width=9, fig.height = 8}
predsOverTime_cv %>%
  ggplot() +
  geom_boxplot(aes(x = name, y = cv, col = name)) +
  theme_minimal() +
  labs(title = "Coefficients of variation of model-predicted  relative cover (all Non-tree types sum to 1) \n at a given location for each functional group from 2010-2020",
        x = "functional group",
       y = "coefficient of variation") +
  theme(axis.text.x = element_text(angle = 90))

# show the locations of each point that is being used for this temporal analysis
#cropped_states <- cropped_states %>%
 # rename(geometry = Shape)

predsOverTime_cv_relative <- predsOverTime_cv %>%
  filter(name %in% c( "relCoverB_C3Grass", "relCoverB_C4Grass" , "relCoverB_Forb",  "relCoverB_bareGround", "relCoverB_shrub"       , "relCoverB_totalHerb", "total_NonTree_AbsCover_CONUS"))
ggplot() +
  facet_wrap(~name) +
  geom_sf(data = cropped_states2, aes()) +
  stat_summary2d(data = predsOverTime_cv_relative, aes(x = x, y = y, z = cv), fun = ~ mean(.x, na.rm = TRUE)) +
  geom_hex(data = predsOverTime_cv_relative, aes(x = x, y = y, col = cv), stat = "mean", na.rm = TRUE) +
  guides(fill = guide_legend(title = "CV")) +
  theme_minimal() +
  labs(title = "The spatial distribution of the coefficients of variation of \nmodel-predicted *relative* cover from 2010-2020 for 1000 random locations",
       subtitle = "Note that values are aggregated across space to enhance readability")
```


## Identify the temporal variability of input variables
```{r}
# get predictors for yes/no forest model
#
decilePredNames_ForestMod <- c(
  str_remove_all(c(str_split(names(coefficients(forestMod)), ":", simplify = TRUE))[-1], pattern = regex("[I\\(]")) %>%
  str_remove(pattern = regex("[2^\\)].")) %>%
  str_remove(pattern = regex("\\)"))
) %>%
  unique()
decilePredNames_ForestMod <- decilePredNames_ForestMod[decilePredNames_ForestMod != ""]

allPredNames <- unique(c(decilePredNames_ForestMod, decilePredNames_totalTree, decilePredNames_NLTree, decilePredNames_BLTree,
decilePredNames_totalHerb, decilePredNames_forb, decilePredNames_C3Grass, decilePredNames_C4Grass,
decilePredNames_shrub, decilePredNames_bareGround))

# make into a long format
predsOverTime_longClim <- predsOverTime %>%
  select(year,x, y, spatialID, uniqueID, allPredNames) %>%
  pivot_longer(cols = c(allPredNames)) %>%
  mutate(year = as.integer(year))

# calculate the coefficient of variation for each cover type at each location
predsOverTime_cvClim <- predsOverTime_longClim %>%
  group_by(uniqueID, name) %>%
  summarize(cv = sd(value)/mean(value))
## add x,y
predsOverTime_cvClim <- predsOverTime_cvClim %>%
  left_join(predsOverTime %>% select(uniqueID, x, y))

# visualize change over time for a selection of points
coverOverTimeForSelectLocations_clim <- predsOverTime_longClim %>%
  filter(uniqueID %in% 1:50) %>%
  filter(name %in% c( #"total_NonTree_AbsCover_CONUS",  "relCoverB_totalTree",
#"relCoverB_totalHerb",
   allPredNames)) %>%
  ggplot() +
  facet_wrap(~uniqueID) +
  geom_area(aes(x = year, y = value, fill = name)) +
  # geom_line(data = predsOverTime_long %>%
  #             filter(uniqueID %in% 1:50),
  #            # filter(name %in% c("relCoverB_totalHerb", "relCoverB_totalTree")),
  #           aes(x = year, y = value, linetype = name), col = "black") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 90)) +
  labs(title = "Change in modeled relative cover (all Non-tree types sum to 1) by functional type from \n 2010 to 2020 for a subset of locations")
mapOfSelectLocations_clim <- predsOverTime_longClim %>%
    select(x, y, uniqueID) %>%
    filter(uniqueID %in% 1:50) %>%
    unique() %>%
    ggplot() +
    geom_sf(data = cropped_states, aes()) +
    geom_point(aes(x, y)) +
    geom_label_repel(aes(x, y, label = uniqueID)) +
    labs(title = "Locations in above plot")


predsOverTime_cvClim %>%
  ggplot() +
  geom_boxplot(aes(x = name, y = cv, col = name)) +
  theme_minimal() +
  labs(title = "Coefficients of variation of model-predicted  relative cover (all Non-tree types sum to 1) \n at a given location for each functional group from 2010-2020",
        x = "functional group",
       y = "coefficient of variation") +
  theme(axis.text.x = element_text(angle = 90))

# show the locations of each point that is being used for this temporal analysis
#cropped_states <- cropped_states %>%
 # rename(geometry = Shape)

ggplot() +
  facet_wrap(~name) +
  geom_sf(data = cropped_states2, aes()) +
  stat_summary_2d(data = predsOverTime_cvClim, aes(x = x, y = y, z = cv), fun = ~ mean(.x, na.rm = TRUE)) +
 # geom_hex(data = predsOverTime_cvClim, aes(x = x, y = y, col = cv), stat = "mean", na.rm = TRUE) +
  guides(fill = guide_legend(title = "CV")) +
  theme_minimal() +
  labs(title = "The spatial distribution of the coefficients of variation of \nmodel-predicted cover from 2010-2020 for 1000 random locations",
       subtitle = "Note that values are aggregated across space to enhance readability")

```

It looks like the most extreme variability occurs in precip_seasonality_anom, prcp_anom, prcpTempCorr_anom, and annWatDef_anom.
Below are the coefficients included in each model:
```{r}
library(flextable)
library(magrittr)
library(data.table)
library(scales)
colourer <- col_numeric(
  palette = c("transparent", "#6aa84f"),
  domain = c(0, 1))
flextable(data.frame("predictor" = allPredNames,
                     "YesNoForest" = allPredNames %in% decilePredNames_ForestMod,
           "TotalTreeCover" = allPredNames %in% decilePredNames_totalTree,
           "NLTreeCover" = allPredNames %in% decilePredNames_NLTree,
           "BLTreeCover" = allPredNames %in% decilePredNames_BLTree,
           "TotalHerbaceousCover" = allPredNames %in% decilePredNames_totalHerb,
           "ForbCover" = allPredNames %in%decilePredNames_forb,
           "C3GramCover" = allPredNames %in%decilePredNames_C3Grass,
           "C4GramCover" = allPredNames %in%decilePredNames_C4Grass,
           "ShrubCover" = allPredNames %in%decilePredNames_shrub,
           "BareGroundCover" = allPredNames %in%decilePredNames_bareGround)) %>%
  theme_vanilla() %>%
    bg(
       j = 2:11,
       bg = colourer) %>%
  color(i = ~ YesNoForest == TRUE, j = ~YesNoForest, col = "white") %>%
  color(i = ~ TotalTreeCover == TRUE, j = ~TotalTreeCover, col = "white") %>%
  color(i = ~ NLTreeCover == TRUE, j = ~NLTreeCover, col = "white") %>%
  color(i = ~ BLTreeCover == TRUE, j = ~BLTreeCover, col = "white") %>%
  color(i = ~ TotalHerbaceousCover == TRUE, j = ~TotalHerbaceousCover, col = "white") %>%
  color(i = ~ ForbCover == TRUE, j = ~ForbCover, col = "white") %>%
  color(i = ~ C3GramCover == TRUE, j = ~C3GramCover, col = "white") %>%
  color(i = ~ C4GramCover == TRUE, j = ~C4GramCover, col = "white") %>%
  color(i = ~ ShrubCover == TRUE, j = ~ShrubCover, col = "white") %>%
  color(i = ~ BareGroundCover == TRUE, j = ~BareGroundCover, col = "white")
```

